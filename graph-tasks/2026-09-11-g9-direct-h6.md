# Task record: G9 direct sixth order on actual clusters (2026-09-11)

## Identity
- Task: `g9-direct-h6-20260911`; agent: Claude Fable 5.1 (Claude Code desktop session, maintainer ats314).
- Checkout: `C:\WORKHOUSE\worktrees\g9-direct-h6-20260911`, branch `claude/g9-direct-h6-20260911`, base `54d1ce6` (origin/main at start).
- Ownership register: `C:\WORKHOUSE\navigation\tasks\2026-09-11-g9-direct-h6.md`.

## Target
- `G9` route `ROUTE:G9:assemble-one-complete-sixth-order-cluste-55f2b8` (frontier priority 6), successor "direct multi-plane H6"; and the ADR 0046 conjecture 4 successor "test the N^-(4k-1) law at k = 3 on the G9 sixth-order clusters".
- Inputs read: `RESULT:G9_HERMITIAN_FOLDED_SUPPORT` (the (F6) word formula), `RESULT:G9_ONE_FACE_SIXTH`, `RESULT:G9_LOCAL_SIXTH_NONCANCELLATION`, ADR 0046 and `runs/planar_band_2026-09-11` (on branch `claude/planar-band-20260911`, unmerged at the time), ADR 0029-0033 (the Q(N) engine), `loopcalc.py`, `symbolic_rank.py`, `chain_cluster.py`, `kernel_orbits.py`.
- Regime: Hamiltonian SU(N) strong coupling, isolated plaquette shell, finite connected clusters, generic rank (Q(N), specialising for N >= 9). No SU(3) determinant family is evaluated.

## Start snapshot
- First briefing `.graph-state/g9-direct-h6-20260911/start.json` (fingerprint `b0793d13…101eab`) was taken through the REPO editable install and observed REPO's dirty Hodge work, not this worktree; retained as an observation, not used.
- Worktree briefing `.graph-state/g9-direct-h6-20260911/start-worktree.json`: status ok, fingerprint `330d68240e2cbc0a8daa37ba6c22fe9bbddd63eef1b88d53a4e90261fc355dfd`, revision `54d1ce6`, clean, freshness `unknown` (fresh worktree, no local index provenance). Targets G9, RESULT:G9_ONE_FACE_SIXTH, RESULT:G9_HERMITIAN_FOLDED_SUPPORT, RESULT:G9_LOCAL_SIXTH_NONCANCELLATION, RESULT:TIER_COLLAPSE_ACTUAL_H4_SUPPORT.

## Obligation
Evaluate the direct sixth-order effective operator on the smallest connected multi-plane support with exact Haar/Gram quotient, electric denominators, all folds and rooted subtraction, and reduce F = sigma(H6); and test whether sixth-order cluster cumulants are O(N^-11) with O(N^-5) channel content.

## Ownership
New: `src/workhouse/sixth_order_cluster.py`, `src/workhouse/sixth_order_characters.py`, `src/workhouse/invariants/sixth_order_cluster.py`, `tests/test_sixth_order_cluster.py`, `runs/g9_direct_h6_pair_2026-09-11/`, this record. Changed: `src/workhouse/loopcalc.py` (Krylov projector, reference kept), `src/workhouse/symbolic_rank.py` (`min_rank`, eight-box label table, monomial fast path), `src/workhouse/invariants/__init__.py`, `runs/index.yaml`, regenerated views. REPO was not touched.

## Work and checks
- Engine: general Bloch recursion with the Hermitian metric on a cluster model space (plaquette sector or vacuum), and the closed word formula (F6) by resolvent powers; a character-basis one-face engine sharing no primitive with the loop calculus.
- Executed: `derive.py validate` (N = 11: recursion == word formula at orders 2, 4, 6 on one face; recursion order 2 == `second_order` and order-4 block == `pair_element` on both shared-link pairs; recursion == character engine at orders 0..6, both C sectors and vacuum). `derive.py single` (one face over Q(N), audit: largest Weingarten family n = 4, largest flux 8, 782 components verified). `pytest tests/test_sixth_order_cluster.py` (8 passed), `tests/test_loopcalc.py`, `tests/test_symbolic_rank.py` (16 passed), ruff clean. Krylov projector compared with the factored reference on every resolvent input of a pair run at N = 3, N = 11 and over Q(N): identical.
- Not executed: the sixth-order pair stages over Q(N) (`pair_perpendicular`, `pair_coplanar`); a symbolic Bloch-metric pair run and an integer N = 11 word-formula pair run were stopped after about 40 minutes at the maintainer's request. No SU(3) sixth-order number was attempted.
- Established result (T1, suite "G9 direct sixth order on one face and the shared-link pairs, any rank"): on the one-face cluster over Q(N), the vacuum-subtracted plaquette energy at order 2k is O(N^-(4k-1)) for k = 1, 2, 3 in both C sectors; at order six every one of the seven (F6) pieces is O(N^-5), their exact sum is the independent character-engine H6 = O(N^-9), and the vacuum subtraction leaves -(10388/3) N^-11 (odd), -2880 N^-11 (even). Three orders cancel, as ADR 0046 conjecture 4 predicts for k = 3.

## End snapshot
No end briefing was taken: the retained snapshot would only reflect this worktree's own regenerated views, and the mathematical inputs (source ledgers) were not changed. Regenerated views are recorded in the commit.

## Handoff
- **Established:** the sixth-order cluster engine, certified three ways at N = 11; the one-face k = 3 confirmation of the N^-(4k-1) law over Q(N) with the named mechanism. This bears on G16 (rank-uniformity in tau) and on G9; it changes no claim status and does not discharge the route.
- **Failed / stopped:** the shared-link pair sixth order over Q(N) is feasible but takes hours at the current engine speed; the Bloch-metric form is much slower than the word formula on a pair (Gram integrals of long words) and should not be used there.
- **Successor obligation:** run `python runs/g9_direct_h6_pair_2026-09-11/derive.py pair_perpendicular pair_coplanar expand certificate` (two processes are fine), then read the pair hop leading powers (prediction: N^-11 with N^-5 pieces) and place the pair hops into the carrier symbol: in-plane orbit pi and rotation orbit rho give sigma = 4 pi q - 2 (pi + rho) e2 (`kernel_orbits.CLOSED_FORMS`), with the rotation sign fixed by requiring the second-order kernel to be pure L_down. Register the pair forms as a RESULT with the cluster hypotheses. The physical SU(3) F needs the pure-six and higher determinant families in `loopcalc.haar_link` (k epsilon groups) or a character-basis pair engine; that remains the route's open step.
