# Reviewed G19 kernel continuation

The source pair connects moving-time spectral exclusion and reconstruction
R1 as reusable ingredients. No new dependency between those older results
is asserted. The new analytic theorem separately constructs the limiting
semigroup from compatible defining kernels, proves totality on its full
defining space, and uses Holder interpolation and spectral positivity.

Register RESULT:OS_KERNEL_MOVING_TIME_GAP and DERIV:OS_KERNEL_GAP groups
KERNEL, LATE_TIME, WEIGHT and LIMITATIONS at their exact source sections.
The result depends on spectral positivity and the existing moving-time
source/budget theorem; the finite controls support only their stated scopes.

The G19 application route stays live. A completed route records only the
kernel/gap implication under explicit hypotheses. Actual Wilson kernel
limits, zero-time continuity, weak-coupling rate, positive source weight and
full Euclidean field structure remain unproved by this task.


## Applied positive-time refinement

The proposal above records the initial K1--K4 stage. The final integration
also registers DERIV:OS_KERNEL_GAP:POSITIVE_HISTORY at K5, with its own exact
control. Positive-time divisibility now discharges the separate zero-time
semigroup-continuity premise under the stated source geometry. Kernel limits,
fixed-time compatibility, weak-coupling rate, nonzero limiting source weight
and the full Euclidean field structure remain actual Wilson obligations.
The earlier stage and its source hashes remain preserved.
