# Archive derivations and targeted local follow-up — 2026-09-11

## Identity and ownership

Codex, isolated checkout `C:\WORKHOUSE\worktrees\archive-derivations-20260911`,
branch `codex/archive-derivations-20260911`, starting main
`ffff7bb2e67b976ba50fe5cf7184966dd581535f`. The canonical checkout and another
agent's untracked task record were left untouched. No shared Lean writer.
Outer coordination record: `navigation/tasks/2026-09-11-archive-derivations.md`.

Owned changes: the two `archive-*` derivation documents, the new
`archive_derivations.py` invariant module and registration, this task and its
evidence directory, `runs/archive_derivations_2026-09-11/`, the new paper note
and its pin, appended document/derivation/result/run records, and generated
proof coverage/catalogue/frontier/certified views. Existing code and historical
scientific records are retained. No new Lean declarations are claimed.

## Target and starting snapshot

Register the supplied synthesis's usable analytic chains at their actual scope,
then recover further derivations by exact local-file search. Targets include
G17, G19, G21 and G23; existing G3, G7 and G9 provenance is preserved.
The regime ranges from exact finite geometry to general conditional analytic
implications; none is registered as a continuum construction.

Starting command: `workhouse brief G19 G23 G17 G3 G7 G9 --json --out .graph-state/archive-derivations/start.json`.
Retained bytes: [start.json](evidence/archive-derivations-20260911/start.json).
Snapshot SHA-256: `1f0f48f1af599435c2b66fd781ca09571dfb1aa8b737375bedc9922a6f869f36`.
Input-manifest SHA-256: `83adc1037922341e081a72e4f581d5cf3c4bbd52e54a0bc330f20e59d81854b9`.
Saved freshness was **unknown** (no local generation receipt); 621 recorded
checks and no new execution. That snapshot alone was not treated as a replay.

## Established sources and local search

The earlier attachment review is retained locally in
`C:\WORKHOUSE\research\second-deep-dive-review_20260911`.
The [source package](../runs/archive_derivations_2026-09-11/README.md) retains
verbatim source bytes, original absolute paths, byte counts and hashes.
The derivation inventory supplies exact document hashes and section/line locators.

Targeted searches and section reads covered:

- `ARCHIVE/collections/02_APPENDICES`: smooth proxy Appendix E, Parts 7 and 8,
  reflected pushforwards Appendix H, finite-range inverse Appendix G, and
  the uniform pairing targets in Appendix I.
- `ALL THEORY/programs/pmbsf`: Section 14 remaining smooth-source LCI,
  far-source stability and capacity obligations.
- `REPO/notes/imported/WORK_SINCE_2026-08`: the August 22 H3 normalized-source audit.
- `ALL THEORY/programs/one_plaquette/glueball_mass_prediction`: the distinct
  full-rank and held-out formulas and their lattice-data provenance.
- Current ledger/result IDs and the retained received synthesis: existing
  B6, sigma5 and G3/C2 results were not re-opened or duplicated.

Exact follow-up source hits include Appendix I's Assumption I.1 at line 68,
(I.5) at 76, (I.8) at 102, (I.9) at 110, and Appendix G's reducing-space
Lemma G.6 at 312 and claimed horizontal-kernel Corollary G.7 at 317.
Queries used `rg -n` with the corresponding theorem labels, `horizontal`,
`projection`, source filenames and fit constants; selected original sections
were then read. A general `workhouse discover search` failed with SQLite's
`database or disk is full`. Its 1.28 GB failed cache was preserved and losslessly
NTFS-compressed to about 615 MB. No cache or source was deleted. The direct
searches completed the requested follow-up without relying on that failed index.

## Derivations and successor obligations

The original archive supplies the core arguments. The registered reconstructions
make assumptions explicit and add narrower repairs/extensions: complete-geodesic
gradient domination, profile calculus, explicit drift constants, normalized
likelihood-ratio transfer, conservative animal counting and common-level RP.
The inverse restriction identity survives; locality of its ambient zero-extension
needs a separate source-localization premise.

Follow-up F16 supplies an all-even-volume center configuration refuting the
specific global pairing targets. F17 derives the signed SU(2) diffusion identity
`Delta V=40D-32V` and negative center-field drift `LW/W=-24kD`. Thus the next
analytic obligation is a uniform off-core estimate retaining the diffusion sign
and controlling pairing/overlap away from centers, together with the appropriate
local inequality. The source, physical-time and continuum-matching obligations
remain separate. These are new deductions, not claims attributed to the archive.

## Validation and end snapshot

The final index rebuild passed at its fixpoint: 17,594 catalogue records,
28 symbol records, 17,622 graph nodes and 29,926 edges. All 31 new derivations,
10 result groups and 18 new checks are present. Graph validation reported no
problems. Current-main ledger records were compared structurally and all
remain unchanged; the [preservation receipt](evidence/archive-derivations-20260911/integration-preservation.json)
identifies the integrated base `bc9da0233ceb72ed76739dc9b892d9c6ca1b50d5`.

The [end snapshot](evidence/archive-derivations-20260911/end.json) was produced by
`workhouse brief G19 G23 G17 G3 G7 G9 RESULT:ARCHIVE_GLOBAL_PAIRING_OBSTRUCTION RESULT:ARCHIVE_SIGNED_SU2_DIFFUSION --json --live`.
Its snapshot fingerprint is
`d64204497c75fd5f1f4517a83f263a5a2fc4cac763cd72478a698db6bbf96de4`;
its input-manifest digest is
`d1ad69c0efaf9e95f8d00910f0226b9e38212185304e27718fa19ac9146a5a70`.
Freshness is **matched**. This briefing reused all 645 byte-matched results,
executed zero new checks and did not compile Lean. Frontier and certified
views also reused those results via the same cache-observation context;
[the receipt](evidence/archive-derivations-20260911/view-generation.json) records
that reuse explicitly. A separate uncached full verifier run is recorded below.

Completed checks so far: 18/18 exact archive controls; source identity for
11/11 retained originals; seven source-inventory integrity tests; ruff lint
and format checks (596 files); documentation validation (38 maintained files,
572 local links, zero errors/warnings); `git diff --check`. The first index
pass also passed before F16/F17 were added. The failed discovery-index attempt,
intermediate encoding repair and merge conflict bytes remain preserved locally.
No stopped process counts as a pass.

Full verifier and pytest outcomes: pending completion before publication.
