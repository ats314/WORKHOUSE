# Unchanged inputs to the marked-shell continuation

These nine proof notes are byte-identical copies from the September 5 research
worktree at the commit recorded in [manifest.json](manifest.json). The manifest
hashes the copied Windows file bytes. The replay also compares each file with
its recorded Git commit, normalizing CRLF to LF only for that comparison.

The [continuation](../wilson-marked-shell-transport.md) lists the exact sections
used and proves the additional complex operator and weighted-kernel estimates.
Some links inside these unchanged historical notes name further documents in
their original repository; their full original paths are recorded in the
manifest. The active calibrated-window and Hamiltonian matching inputs are in
the current repository's paper/research_notes directory and are hashed in the
continuation's validation record.

These are analytic proof inputs. Byte verification establishes provenance;
it does not establish a theorem's mathematical truth.
