# 36. A local creator inverse controls the Wilson transfer

Date: 2026-09-05. Status: accepted.

## Context

The actual symmetric Wilson vacuum, its uniformly gapped auxiliary parent,
and common-filter spectral flow are established. Partition inversion also
constructs exact connected activities of that chart. Its generic
quasi-locality estimate does not supply the exponential cardinality norm
required by the excited-window bridge.

## Derivation and decision

Invert the creator tangent map directly. Exact-support lowering deletes
its whole support; every surviving output root belongs to another
creator. The resulting rooted contraction solves `b-T_w b=dot w` by a
real-linear Neumann series, giving an anti-Hermitian local generator.
Its unitary carries the product vacuum to the actual vacuum line, with
the scalar phase retained explicitly.

A holomorphic doubled system treats the conjugate creator coordinates
independently. Connected active plaquette witnesses then control the
full assigned interaction supports, including coupling dependence that
would be lost by recording only a smaller exact excited support.
The actual Perron logarithm has a connected local expansion as well.

Combine both unitary legs, magnetic insertions and the scalar logarithm
in an ordered contour with kinetic contraction factors. Disjoint
components factor by shuffling their ordered times. A rooted-tree
supersolution bounds the connected component sum. On
`|u|<=u_star/1252800000`, the actual activities satisfy

```text
sup_i sum_(X contains i) 2^|X| ||F_X|| <=1/2500.
```

The excited-window bridge gives the actual normalized-transfer bound
`1/998`, hence the complete finite physical charge-odd shell on a
volume- and mesh-independent interval. The chart also transports local
sources with cardinality and spatial margins.

Record the generic tangent inversion and generic ordered-contour bound
as independent analytic result nodes. Connect their Wilson specialization
to the actual symmetric creators, connected coefficient locality and
scalar endpoint normalization. Close the weighted route by this
sufficient replacement chart. Its use of the earlier extraction note is
the generic partition uniqueness lemma, not an identification with that
note's common-filter activities or excited action.

## Consequences and remaining target

The new norm estimate controls the actual Wilson transfer, unlike the
auxiliary parent gap. The finite contour is complete independently of
source overlaps. The next task is its thermodynamic complete Riesz range
and the uniformly invertible, total projected literal-source frame.
Local source conjugation bounds are inputs to that task, not its result.
G18 and G19 retain those obligations and the later temporal and continuum
matching hypotheses.

The proofs are the
[cardinality chart](../../paper/research_notes/G18_WILSON_CARDINALITY_UNITARY_CHART_20260905.md)
and [weighted activity theorem](../../paper/research_notes/G18_WILSON_WEIGHTED_ACTIVITY_BOUND_20260905.md).
Exact finite tensor controls and rational majorant checks support their
specified algebra and arithmetic. The uniform Hilbert-space, contour and
thermodynamic source arguments retain analytic evidence. Earlier sealed
papers and runs preserve the stage at which they were written.
