# Universal cellular Hodge repair, 2026-09-11

The user authorized resolving the prior review's repairable findings. The
corrected source is `docs/derivations/universal-cellular-hodge-tetrahedral.md`.
Original submitted bytes, graphs, and source records are preserved in
`received/`, pinned by `received-manifest.json`. Navigation byte preservation
is recorded separately in the outer workspace. The previous review remains
historical evidence; its no-publication verdict applied to the received version.

Established: cellular incidence identities, the exact tetrahedral face
commutant, and the all-power shifted operator identity. Three new Lean
operator/recurrence proofs supplement the five submitted valid lemmas.
The physical 60-history identification remains open; the current invariant
now checks the precise flux-count and face-projector distinction. U3 and U7
remain conjectured with established ingredients. Source copies, hashes,
formal coverage, and current research entry points have been reconciled.

Validation is in progress. Final commands/results, end briefing, and publication
state will be appended after the required checks. No current publication is
claimed by this initial receipt.

## Observed verification and integration repair

The strict exporter completed 3630 Lean build jobs and audited 439 registered
theorems / 910 declarations. `workhouse verify` completed **597/597 passing
checks** without reading cached verdicts (`verify.log`). The first full
`scripts/check.sh` run reported **1926 passed, 4 Windows symlink skips,
11 subtests passed, and 2 failures** in 379.43 seconds (`check.log`). Both
failures concerned these new evidence packages: missing run registrations
and SHA256SUMS manifests. No mathematical or proof regression failed.

The integration correction adds both run entries and complete file pins.
The paper manifest retains its original LF convention without changing any
declared digest, and the combined tetrahedral source locator now includes
both the incidence and commutant sections. Full validation is rerun after
this integration correction; the final result is recorded with publication.

The original paper manifest is named `SHA256SUMS.received.txt` inside
`received/`: it is preserved documentary input rather than an active manifest
for a partial copied paper directory. Its bytes still match the intake hash.

## Integration with current main

Main advanced to `54d1ce6` through the independent moving-time spectral-gap
result. A named stash and raw 79-file checkpoint retain the complete repair
before the fast-forward and source merge (`integration.json`). Both source
additions are retained. The latest index attempt wrote a validated catalogue
but rejected its freshness record because the run register newline correction
occurred during generation (`index-changed-inputs.log`); it is not counted
as a successful regeneration. Final generation and validation use the stable
combined inputs. The strict Lean export remains current: the incoming main
change adds no Lean source or theorem-registration changes.
