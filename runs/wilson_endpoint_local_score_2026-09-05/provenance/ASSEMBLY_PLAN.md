# Sealed endpoint, local-score and covariant-source run plan

Authorized new run: `runs/wilson_endpoint_local_score_2026-09-05`.
Root owns run registration, proof pins, result supports and generated views.
The assembler owns only this new run and outputs assembly helpers. No
existing run, original evidence or final canonical proof may be rewritten.

The canonical documentation copy audit records four final proof hashes.
Wait for the native owner's final 15-source handoff, then run the canonical
fresh-only verifier to a fresh outputs certificate. Expected schema is
`workhouse-endpoint-local-source-controls/v1`, with five original payload
families, ten scoped checks and eight selected native modules. Preserve
all verifier SOURCES under `source/` with repository-relative paths.

`ORIGINAL_INPUT_INVENTORY.json` verifies all twelve declared source pins
of the five original reports. Copy the exact originals, final reports and
audits to the run root using their existing basenames, so every canonical
link resolves. Also preserve their required repository-relative layout
under `original_tree/`. The original endpoint replay uses parents[5] to
locate its four prior canonical inputs; its preserved path depth and all
four pinned prior note bytes must be present in this isolated tree.
The other controls use sibling source pins or their own script hash.
Earlier draft reports are unnecessary and should not be represented as
current replay evidence. Preserve one if specifically useful only with
its complete historical source snapshot and scope.

The new frozen replay must set `sys.dont_write_bytecode=True` before imports,
block NumPy/SciPy, and load only the explicitly pinned native model,
aggregate and invariant files through empty private package namespaces.
Import the untouched originals and recompute each complete mathematical
payload. The local-gradient report stores its payload at top level after
removing only `source_sha256`; the other four use `controls`. Replay all
declared source hashes and native check records, not just a passed flag.
Run the original endpoint read-only replay from its preserved tree too.

Before sealing, exercise malformed mathematical certificates, a missing
complete tail or wrong full floor, changed report values, source corruption,
optimized mode and existing-output rejection. Any disposable mutation must
be confined to a verified temporary copy of the new run. Cold relocation
must compare a fresh selected-tree native certificate, block reads of the
original checkout's source files, allow only declared interpreter libraries,
and leave every relocated source byte unchanged. No optimizer or numerical
eigensolver is used. Source hashes must agree before and after acceptance.

Write concise README reproduction/scope instructions, the native certificate,
complete replay evidence and provenance. Capture source and documentation
owner audits as available before the final seal. Inventory every file
except the manifest itself, excluding bytecode caches, then verify the
exact file set and SHA256 of every entry. Resolve all 24 currently pending
canonical run-link occurrences before declaring the run final. Record the
manifest hash, certificate schema/counts and cold replay result for root.

`RESULT_SUPPORT_MAPPING.json` proposes eleven narrow check edges spanning
all ten native checks plus one run support for each of five analytic
results. Root should add run edges only when the final sealed run exists.
The repeated finite cluster check supports the generic mechanism and the
actual application's mechanism separately; its actual Wilson limit scope
is explicitly excluded from finite certification. No Lean support is added.
