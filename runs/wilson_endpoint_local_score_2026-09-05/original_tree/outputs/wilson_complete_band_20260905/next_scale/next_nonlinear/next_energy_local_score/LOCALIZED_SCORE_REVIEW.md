# Localized true-ground score: proof and finite-control review

5 September 2026. Outputs only. No canonical file or sealed run was changed.

The parent and merge_consistency independently accepted the mathematical
argument in TRUE_GROUND_LOCALIZED_WILSON_SCORE.md. The final statement holds
for every fixed SU(N), N>=2, with N-dependent thresholds and constants.
The explicit cross-product finite control is SU(2).

The substantive new step is the derivative-controlled expansion of the true
joint Wilson ground, followed by normalization as a vector in fiber L2.
The already established first-order actual metric operator kills the
leading invariant Gaussian. Arbitrary finite quasimode order and a fixed
compact elliptic bootstrap therefore give the required coarse derivatives
and weighted fiber tails with O(g^2) remainder. Normalizing the Hilbert
vector cancels the scalar leading coarse derivative, using only a marginal
lower bound on a fixed rescaled compactum.

This establishes actual intrinsic Fisher boundedness on that compactum.
Its leading form is 49/(72 sqrt(5)) ad_Q^*ad_Q. Existing exact centralizer
geometry then gives the sharper O(g^2) Fisher contraction along class
gradients. The actual vacuum-subtracted literal fast form supplies the
remaining inverse factor:

- Localized single-strip class sources: relative Schur loss O(g^4)=O(u^-1).
- The complete cutoff radial/pair chart under one common Gauss constraint:
  relative loss O(g^2)=O(u^-1/2), uniformly in finite/countable additive copies.

The copied blocks have no interacting boundary terms. The selected source
chart is onto the specified complete fine window, but is not asserted to
equal a marginal spectral window. A selected-space Schur comparison alone
does not exclude low states descending from the high retained complement.

The five finite control families check the 7/24 metric residual, cancellation
of the order-g forcing on the invariant Gaussian, Fisher normalization and
radial null contraction, common-Gauss pair variance, and the two scalar
Schur exponents. They do not certify compact elliptic estimates, actual
ground-state asymptotics, source-frame totality or countable form domains.
Ruff passed. Exact recorded replay passed, existing-output generation was
rejected, optimized execution was rejected, and a deliberately corrupted
common-Gauss covariance was rejected. Certificate bytes were unchanged by
replay. Final pins and test exit codes are in LOCALIZED_SCORE_VALIDATION.json.

I also independently reviewed the companion
../next_local_gradient_tensorization/LOCAL_GRADIENT_EXCITATION_SUPPORT.md.
Its local gradient support orthogonality, exact radial/pair form matrices,
countable form closure, any-bad counterexample and bounded-cutoff onto frame
are valid as stated. In particular arbitrary off-diagonal entries of the
local bad form do not create cross-support terms. The new local Fisher
estimate supplies the actual score premise for that companion's cutoff
source chart; it does not change its limitation to additive blocks.

The failed equal-frequency bouquet shortcut was not used: one literal
radial product-holonomy source cannot cover its three degenerate first
physical fine modes. The actual unequal-frequency adjacent strip is the
geometry used in the theorem and its complete fine-window application.
