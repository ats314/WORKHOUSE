# Unchanged spatial analytic inputs

These copies preserve the September 5-7 proofs used by
[the spatial Schur-excess continuation](../wilson-spatial-schur-excess.md).
The adjacent manifest records their originating checkout, full commit and
SHA-256 of the copied bytes. The validator also compares each copy with the
recorded Git blob, allowing only CRLF/LF normalization in that comparison.
These documents retain their original relative links and stage metadata;
their originating checkout supplies links outside this selected input set.

The closed-form Schur theorem supplies the full energy and graph-source
comparison. The endpoint and full-Green notes identify the nonreducing
quadratic baseline. The dynamic cubic note bounds a selected localized
forcing. The finite-cell and cubic-ground notes supply the actual local
Wilson jets and ground normalization. The harmonic obstruction and
flat-holonomy/source repair prevent deletion of retained slow variables or
unjustified differentiation of a changing physical rank.

The main continuation states exactly which hypotheses remain open. Preserving
these bytes does not promote an analytic proof to machine certification.

Replay from the repository with `PYTHONPATH=src`:

```text
.venv/Scripts/python.exe scripts/validate_wilson_spatial.py
```
