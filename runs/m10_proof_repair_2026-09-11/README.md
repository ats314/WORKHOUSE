# M10 proof repair evidence

The original completion claim at commit 85675ed is preserved under `preserved/`;
`preservation.json` records exact original paths and SHA-256 values. These are
historical assertions, not the corrected mathematical status. The intermediate
criterion-only source is retained under `before-actual-complement/`.

The maintained derivation establishes the actual centered conditional complement
estimate, the compact action gap outside the full minimizing set, and the exact
reference angular crossover budget. It supplies sufficient amplitude and assembly
criteria. Actual M10 remains open for polynomial relative-amplitude control,
tube moments, and the actual normal/angular comparison and score error.

The retained start snapshot describes the pre-repair graph and reports unknown
saved freshness; it does not execute checks. Validation receipts and publication
outcomes are linked from [the task record](../../graph-tasks/2026-09-11-m10-proof-repair.md).
The pin below covers this frozen archive; later validation receipts are kept with
the task so that adding a receipt does not change the inputs it describes.
