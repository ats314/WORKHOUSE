# Independent audit of the actual full-vacuum complement and Schur form

5 September 2026. The final outputs proof
`ACTUAL_TWO_SQUARE_FIBER_COMPLEMENT.md`, SHA256
`d4dbda99173a12b49fcdd05541f2639821f06cf4537e6c3adf4f4eb3aa7a7da5`,
is accepted at its fixed-rank, one-adjacent-strip scope. The statement is the
bottom of the complete restricted physical form above the true full vacuum,
not the internal gap of that compression.

For each fixed u, positivity and global compact ellipticity make the exact
conditional ground kernel smooth. Its decomposable P and Q preserve H1 and
the diagonal physical Gauss constraint. The constrained form is dense, closed
and has compact resolvent. No uniform global conditional gap or derivative
bound is needed for these fixed-u domain assertions.

The lower bound uses precisely the complete rank-two physical spectral space
below the mixed shell. Harmonic eigenvector convergence, coarse potential
tightness and the near-I ground-bundle estimate imply `||Q E_<||=o(1)`.
Consequently the exact spectral inequality yields

```
Q(H-E_vac)Q >= (E2-E0)(1-||Q E_<||^2)
```

on the constrained form domain. This cannot miss a lower state merely because
it has no overlap with a chosen source. No convergence rate is necessary for
the leading sqrt(u) coefficient.

The upper bound also controls energy. The Haar-flattened mixed Gaussian has an
Ad-equivariant fiber factor exactly odd under inversion and therefore exactly
orthogonal to the central conditional ground. The true ground's O(|X|^2)
change and O(|X|) derivative give a removed component with norm O(g^2) and
unscaled energy O(g^2). The exact projected-form identity includes its full
fiber tail. Form Cauchy-Schwarz then bounds the cost of enforcing Q by O(1).
The resulting coefficient is the physical mixed energy sqrt(3)+sqrt(5), not
sqrt(5) or the independent class-fiber coefficient 2sqrt(5).

Section 6 genuinely realizes the infinite-retained-space Schur hypotheses for
this actual block. Fiber orthogonality kills the vertical cross form and gives
`<varphi,D_b q>=-<D_b varphi,q>`. One coarse Haar integration by parts removes
the derivative of the retained coefficient from the remaining cross term.
Fixed-u smoothness gives a bounded L2-to-H1 cross functional. The positive
actual F floor and full ellipticity make its form norm equivalent to H1 on Q.
Riesz representation supplies bounded U and B=F^(1/2)U at fixed u.

The subtraction `k0=h_PP-||Bp||^2` is bounded in retained L2, so it preserves
the closed coarse domain. Testing q=-Up proves nonnegativity, and square
completion has exactly the original triangular H1 domain. The normalized
coarse form has compact resolvent by bounded invertible pullback of the compact
coarse embedding; no unproved preservation of H1 by M^(-1/2) is required.
Kernel correspondence keeps the unique actual vacuum. The inherited spectral
inequalities and entire low-window graph frame therefore apply as stated.

These arguments do not bound U uniformly in u or in volume, identify the Schur
operator with a prescribed bare Wilson Hamiltonian, or identify graph vectors
with a literal/OS source algebra. The retained conditional line need not
contain the full vacuum. The earlier product-vacuum counterexample remains
relevant to any many-block extension. The global vertical-barrier theorem is
complementary; its simple unitary potential inequality is reproved directly
and is not an extra theorem dependency of the complement proof.

The finite control source was inspected and `controls()` independently replayed
without changing any files. Its rational four-state families attain the leakage
bound and projection-energy scaling while the true vacuum has nonzero Q part.
The physical mixed polynomial has the exact asserted Gauss invariance, parity
and oscillator energy. These controls support the finite mechanism, not the
actual Wilson projection limit or the unbounded-form theorem. Verified hashes:

- Script: `27ca1efc05989af4a80d0c591d07c9bee986078ce9ea059d46b8879223989db2`.
- JSON: `d8f0ab85737fd0f67fbe8b595183fbeb054710b0f3c905be0d4198278024a519`.
