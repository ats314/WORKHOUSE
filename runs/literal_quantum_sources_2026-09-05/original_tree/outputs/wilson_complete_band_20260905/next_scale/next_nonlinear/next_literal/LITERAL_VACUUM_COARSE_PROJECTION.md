# Literal coarse sources based on the true Wilson block vacuum

5 September 2026. Independent outputs-only derivation. No current canonical
source, native check or sealed run is changed. This note concerns the actual
seven-link two-square block and then additive copies with independent Gauss
constraints. It does not identify the retained equal-time source range with
the reducing range generated by all OS histories.

## 1. The exact ground-based source isometry

Fix SU(N), N >= 2, and u > 0. On Haar L2(G^2), with simultaneous-conjugation
physical subspace, use the actual operator

```
H_u = -(1/2)[3Delta_1+3Delta_2+sum_a(L_1,a-R_2,a)^2]
      +2u[v(U1)+v(U2)],       v(U)=N-ReTr U.
```

Its original-link derivation and physical low spectrum are established in
`paper/research_notes/G19_WILSON_TWO_SQUARE_PHYSICAL_SHELLS_20260905.md`.
Uniform ellipticity on the compact connected manifold implies a unique
normalized strictly positive smooth ground Omega_u, with energy E0(u).
Uniqueness makes it gauge invariant, so it is also the physical vacuum.
Write h_u=H_u-E0(u) as a nonnegative closed quadratic form.

The global variables are U=U1 U2, K=U1, with product Haar measure dU dK.
Define the true quantum marginal and source map

```
mu_u(U) = integral Omega_u(U,K)^2 dK,
J_u f(U,K) = f(U) Omega_u(U,K),
J_u : L2(G,mu_u dU)^Ad -> H_phys.                       (1)
```

Here mu_u is smooth, strictly positive and conjugation invariant, and
integral mu_u dU=1. Thus J_u is an isometry with closed range. Its adjoint
and range projection are exactly

```
J_u* psi(U) = mu_u(U)^(-1) integral Omega_u(U,K)psi(U,K)dK,
P_u psi = Omega_u J_u*psi,          Q_u=I-P_u.           (2)
```

The formulas preserve the physical subspace. In particular

```
J_u 1=Omega_u,       P_u Omega_u=Omega_u,       Q_u Omega_u=0. (3)
```

The range is precisely the closure of coarse class observables applied to
the true vacuum. Smooth class functions are dense in L2(mu_u dU)^Ad, so
this is a literal equal-time source space. It is not obtained by replacing
mu_u with Haar measure or the raw Wilson conditional density.

For each fixed u, mu_u has a positive minimum on the compact coarse group.
This fixed-u fact gives H1 preservation by P_u,Q_u: their normalized fiber
kernel is phi_u=Omega_u/sqrt(mu_u), smooth with bounded derivatives. The
bounds may depend on u. Applying Q_u to smooth invariant approximations
shows that H1 cap ran Q_u cap H_phys is dense in ran Q_u cap H_phys.
The restriction of h_u to this domain is closed and has compact resolvent.
Its associated operator F_u is the form compression, not an assumed
operator-domain product Q_u h_u Q_u.

No u-uniform positive lower bound on mu_u is used below.

## 2. Exact retained ground-state form, including the true vacuum shift

The exact ground-state transform on smooth functions is

```
h_u[Omega_u F]
 = (1/2) integral Omega_u^2 <grad F,C grad F> dU dK,     (4)
```

where C is the original-link cometric in the unbalanced variables. This
follows by expanding the electric divergence form, integrating by parts
and using H_u Omega_u=E0 Omega_u. Compactness and positivity extend it to
the weighted Sobolev form domain. It is an identity of the actual quantum
Hamiltonian after its true ground energy has been subtracted.

For the actual strip, the coarse block of the cometric is independent of K:

```
C_uu(U)=8I-Ad(U)-Ad(U)*.                               (5)
```

This is the same original-link matrix as in the exact strip decomposition
in `G19_WILSON_STRIP_BO_AND_TWO_STRIP_SPLITTING_20260905.md`, Section 1.
For F=f(U), all fiber derivatives vanish. Integrating K in (4) gives

```
h_u[J_u f]
 = (1/2) integral <grad f,C_uu(U)grad f> mu_u(U)dU.      (6)
```

Its domain is H1(G,mu_u dU)^Ad. At fixed u this equals the usual coarse
H1 set with an equivalent norm. The generator in the weighted coarse
space is the self-adjoint divergence-form realization

```
A_mu f = -(1/(2mu_u)) sum_ab E_a(mu_u C_uu,ab E_b f).   (7)
```

The coarse invariant derivatives E_a are Haar skew. Equation (7) is meant
initially on smooth class functions and then through its closed form.
There is no extra scalar potential in this weighted representation.
Conjugating to Haar space introduces the corresponding marginal-dependent
terms; dropping them would change the operator. Equation (6) is an exact
compression, not a reducing-subspace assertion or a Markov property for
the observed quantum histories.

## 3. Low-space approximation needs bounded cutoffs, not density ratios

Let g=u^(-1/4). The established actual physical shell theorem and its
localization proof give, in balanced coordinates

```
U=exp(sqrt(2)g q),   K=U^(1/2)exp(g z/sqrt(2)),
```

after the exact local Haar half-density flattening and rescaling,

```
Omega_u -> Omega_0=Phi_q(q)Phi_z(z),
psi_1(u) -> p_1(q)Omega_0,
p_1(q) proportional to |q|^2-d sqrt(3)/2,  d=N^2-1.    (8)
```

The phases are chosen consistently and all vectors are normalized.
Phi_q and Phi_z are the oscillator Gaussians of frequencies sqrt(3)
and sqrt(5). The local comparison is accompanied by L2 tightness, so
discarding the complement of a fixed unscaled chart costs o(1).

Choose p_1,R(q)=chi(|q|/R)p_1(q), with chi smooth, compactly supported,
radial, and equal to one near zero. For each fixed R, define the globally
smooth class function f_u,R(U)=p_1,R(log(U)/(sqrt(2)g)) within the coarse
chart and zero outside. At small g its support is strictly inside the
chart and ||f_u,R||infinity <= ||p_1,R||infinity, independent of u.

Multiplication by f_u,R commutes exactly with the local Haar flattening.
With the chart cutoffs included, the triangle inequality therefore gives

```
limsup_(u->infinity) ||psi_1(u)-f_u,R Omega_u||
 <= ||(p_1-p_1,R)Omega_0||.                            (9)
```

Indeed the other terms are the L2 eigenvector error in (8), the bounded
multiplier norm times the L2 vacuum error in (8), and chart tails of the
same vectors. For each fixed R these all vanish. Then Gaussian moments
give ||(p_1-p_1,R)Omega_0|| -> 0 as R -> infinity. This ordering of limits
is essential: L2 convergence alone does not justify multiplication by the
unbounded polynomial p_1.

Since every f_u,R Omega_u is in ran P_u,

```
delta_u := ||Q_u psi_1(u)|| -> 0.                      (10)
```

The complete physical spectral space below the mixed shell consists of
Omega_u and this simple first eigenvector, for sufficiently large u.
Let Pi_low,u be its rank-two orthogonal projection. Equation (3) gives
the sharper identity

```
||Q_u Pi_low,u|| = delta_u.                            (11)
```

No division by Omega_u or lower bound on mu_u appears in this approximation.
The normalized conditional fiber line need not converge uniformly in U.

## 4. Complement floor and literal low-window totality

Write a_u=E1(u)-E0(u), b_u=E2(u)-E0(u). The complete physical spectral
theorem, retaining the first excited energy, gives

```
h_u >= a_u |psi_1><psi_1|
        +b_u(I-|Omega_u><Omega_u|-|psi_1><psi_1|).      (12)
```

For a unit form vector in ran Q_u the vacuum coefficient is exactly zero,
and its psi_1 coefficient has modulus at most delta_u. Therefore

```
F_u >= [b_u-(b_u-a_u)delta_u^2] I.                     (13)
```

This improves b_u(1-delta_u^2) by keeping the known first-level energy.
It is at least a_u even without an asymptotically small delta_u. From
(8)--(10) and the established shell asymptotics,

```
liminf_(u->infinity) inf spectrum(F_u)/sqrt(u)
 >= sqrt(3)+sqrt(5).                                  (14)
```

Thus the actual true-vacuum-subtracted literal complement has the desired
mixed-shell lower floor. No upper trial is needed for this conclusion.
This note does not claim an equality in (14).

The literal sources also cover the whole low rank-two window. In its basis
Omega_u,psi_1, vacuum orthogonality gives exactly

```
Pi_low P_u Pi_low = diag(1,1-delta_u^2).                (15)
```

For delta_u<1, B_u=Pi_low J_u is onto ran Pi_low and has right inverse
B_u*(B_u B_u*)^-1, of norm at most (1-delta_u^2)^(-1/2). This uses the
positive lower bound on B_u B_u*, not an invalid inference from a lower
bound on B_u* B_u. The same argument will work for the infinite-rank
independent-copy low sector below.

## 5. Fixed-u actual Schur realization and its vacuum line

The lower floor in (13) and H1 preservation allow the actual closed-form
Schur construction at each fixed sufficiently large u. The cross bound
needed for the infinite-dimensional retained space follows directly from
the ground-state transform, without a conditional rotor eigen-equation.

For a smooth q in ran Q_u, put G=q/Omega_u. Fiber orthogonality says
integral Omega_u^2 G dK=0. Since C_uu and C_uK depend only on U, differentiating
this identity in a coarse direction and integrating Haar-skew fiber
derivatives gives

```
integral Omega_u^2 E_b G dK = -2 integral (E_b Omega_u) q dK,
integral Omega_u^2 L_b G dK = -2 integral (L_b Omega_u) q dK.
```

Consequently the polarized cross form is

```
h_u[J_u f,q] = - integral sum_ab conjugate(E_a f)
   [C_uu,ab <E_b Omega_u,q>_fiber
      +C_uK,ab <L_b Omega_u,q>_fiber] dU.              (16)
```

The ground is real, so these fiber brackets are unambiguous. Integrating
the remaining coarse derivative by parts bounds the result by
C(u)||f||L2(mu_u)||q||H1. Only fixed-u bounded derivatives and the fixed-u
positive minimum of mu_u are used here. There is no second derivative of f
left in the cross estimate.

The complementary form norm is equivalent to H1 at fixed u because of
ellipticity, nonnegative magnetic potential and the positive floor (13).
Riesz representation gives a bounded lift T_u from the retained L2 space
into the complementary form domain, satisfying

```
f_Q[T_u J_u f,q]=h_u[J_u f,q].                         (17)
```

As in the established form-Schur theorem, subtraction of the bounded
square from the retained form gives a closed nonnegative k0, and

```
h_u[p+q]=k0[p]+||F_u^(1/2)(q+T_u p)||^2.              (18)
```

Its domain is exactly p in D(k0), q+T_u p in D(F_u^(1/2)), since P,Q
preserve H1 and T_u maps retained L2 into complementary H1. The retained
part before this Schur correction is precisely (6).

The true vacuum is particularly simple: h_u[Omega_u,q]=0 implies
T_u Omega_u=0. Thus the induced mass M_u=I+T_u*T_u fixes the vacuum,
and the normalized Schur graph source carries the retained constant
function exactly to Omega_u. The static Schur form has the same one-
dimensional vacuum kernel. This resolves the raw product-vacuum mismatch
for this source choice. It does not bound T_u uniformly in u or volume,
nor identify the Schur-corrected operator with a bare Wilson Hamiltonian.

## 6. Uniform tensorization for independent Gauss blocks

For M identical noninteracting blocks, each with its own Gauss constraint,
let the Hilbert space be the tensor product of their physical spaces,
the nonnegative Hamiltonian be h_M=sum_i h_u,i, and the vacuum be
Omega_M=Omega_u^tensor M. The true measure, literal isometry and projection
factor exactly:

```
mu_M=mu_u^tensor M,       J_M=J_u^tensor M,
P_M=P_u^tensor M,         Q_M=I-P_M,
P_M Omega_M=Omega_M.                                  (19)
```

For sufficiently large u, 2a_u >= b_u, since 4sqrt(3)>sqrt(3)+sqrt(5).
Then the complete spectrum below b_u consists of the vacuum and the M
orthonormal one-excitation vectors

```
w_i=Omega_u tensor ... tensor psi_1 tensor ... tensor Omega_u.
```

Let R_M project onto their span, and Pi_M=|Omega_M><Omega_M|+R_M.
Because P_u Omega_u=Omega_u,

```
Q_M w_i=Omega_u tensor ... tensor (Q_u psi_1) tensor ... tensor Omega_u.
```

The vectors on the right are mutually orthogonal: Q_u psi_1 is exactly
orthogonal to Omega_u. Each has norm delta_u. Hence

```
||Q_M Pi_M||=delta_u,      R_M Q_M R_M=delta_u^2 R_M.   (20)
```

There is no sum of errors proportional to M. Applying the complete
additive spectral theorem and using Q_M Omega_M=0 gives

```
Q_M h_M Q_M >= [b_u-(b_u-a_u)delta_u^2] I_(ran Q_M),   (21)
```

as a restricted-form inequality. The lower constant and the sufficient
large-u threshold are independent of M. Similarly
Pi_M P_M Pi_M is exactly one on the vacuum and 1-delta_u^2 on R_M.
The literal projected source is onto this complete (M+1)-dimensional low
window with the same frame bound as in (15).

This is a genuine uniform-in-copy-count result for the additive model.
It relies on the exact vacuum inclusion and the complete low sector,
not tensorization of approximate vacuum overlaps. Independent Gauss is
part of the hypothesis; shared Gauss permits charged cross-block singlets
and requires its own low-sector accounting.

## 7. Countably many independent copies

The same conclusion holds in the incomplete tensor product Hilbert space
based on the exact vacuum Omega_u, equivalently the product-vacuum GNS
representation of independent block algebras. Define h_infinity as the
nonnegative sum on the finite-excitation core and take its self-adjoint
closure through the tensor spectral decomposition.

Since the single-block gap a_u is positive, every nonvacuum excitation
costs at least a_u. The full low subspace below b_u is therefore the vacuum
plus the l2 direct sum of the single psi_1 excitations when 2a_u>=b_u.
All other tensor spectral sectors have energy at least b_u.

The finite products of P_u decrease strongly to an orthogonal projection
P_infinity. On finite-excitation vectors they are eventually constant,
because every untouched factor is the exact retained vacuum. Its range
is the incomplete tensor product of the retained spaces; J_infinity is
the corresponding isometry. Equations (20),(21) pass directly to this
decomposition, with exactly the same delta_u and lower floor.

The restricted form is densely defined: applying Q_infinity to the
finite smooth-excitation core stays in a finite product form domain and
is L2 dense in ran Q_infinity. Restriction of the closed form is closed.
No claim that Q_infinity is uniformly bounded in the entire energy-form
norm is needed for this domain argument.

The low projection now has infinite rank, but its literal frame still
satisfies Pi P_infinity Pi >=(1-delta_u^2)Pi. Thus Pi J_infinity is onto by
the B B* inverse argument, not merely bounded below on its source domain.

This independent-product construction has no ambient shared links or
plaquette interactions. It supplies a precise test model and removes one
earlier obstruction; it is not an interacting Yang--Mills thermodynamic
limit or a continuum mass theorem.

## 8. Evidence boundaries and next consequence

`check_literal_vacuum_projection.py` separately checks a nonuniform exact
ground-state marginal/isometry and discrete weighted form; a rational
four-state model attaining the refined floor; finite tensor low-space
leakage; and a counterexample showing why unbounded multipliers cannot
be passed through L2 vacuum convergence without cutoffs.

The general cutoff, elliptic-domain, spectral and independent-product
arguments are analytic. These outputs do not alter the current canonical
package. The next coupled problem is to control the true interacting
vacuum marginal and its coarse energy form, together with the mixed
physical low sector and Schur lift, uniformly under block coupling.
