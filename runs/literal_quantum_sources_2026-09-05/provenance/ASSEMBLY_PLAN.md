# Next literal-source and Gaussian run: assembly plan

5 September 2026. Root has authorized the NEW run
`runs/literal_quantum_sources_2026-09-05` on
`codex/literal-quantum-scale-sources-20260905`. Assembly waits only for final
five-note/native source freeze handoffs. Earlier sealed evidence stays unchanged.

## 1. Mathematical scope and proposed registration

The five accepted canonical proof families are distinct; the inverse-energy
addendum strengthens the literal/common results without adding a result node:

| Original proof | Proposed canonical research note | Proposed result IDs |
|---|---|---|
| `next_literal/LITERAL_VACUUM_COARSE_PROJECTION.md` | `G19_WILSON_LITERAL_VACUUM_COARSE_SOURCES_20260905.md` | `WILSON_LITERAL_FAST_COMPLEMENT`, `WILSON_LITERAL_COARSE_FORM` |
| `next_literal_common/COMMON_GAUSS_LITERAL_FAST_FLOOR.md` | `G19_WILSON_COMMON_GAUSS_LITERAL_FAST_FLOOR_20260905.md` | `WILSON_COMMON_GAUSS_LITERAL_FAST_COMPLEMENT` |
| `next_literal/GROUND_MARGINAL_SCHUR_SCORE.md` | `G19_GROUND_MARGINAL_SCHUR_SCORE_20260905.md` | `GROUND_MARGINAL_SCHUR_SCORE` |
| `next_gaussian_full/ENTIRE_GAUSSIAN_LITERAL_COMPLEMENT.md` | `G19_GAUSSIAN_QUANTUM_FAST_SOURCES_20260905.md` | `GAUSSIAN_QUANTUM_FAST_SOURCES` |
| `next_quantum_score_center/TRUE_GROUND_CENTER_SCORE_OBSTRUCTION.md` | `G19_TRUE_GROUND_CENTER_SCORE_OBSTRUCTION_20260905.md` | final center-obstruction ID supplied by documentation owner |

`next_literal_inverse/LITERAL_INVERSE_ENERGY_FULL_FORM.md` is separately
provenanced in the new canonical literal note, with a common-Gauss crosslink.
It proves full form domination and entire low-window onto frames. The center
proof disproves the old global score candidate, retaining the generic theorem
and a precisely conditional energy-localized replacement.

Each ID receives the `RESULT:` prefix. Canonical paths are under
`paper/research_notes/`. The staged metadata in
`../staged_docs/results.yaml` and `../staged_docs/documents.yaml` must be
reviewed and handed off by their owner; support fields remain empty until
real native IDs and the final run exist.

The scope is actual fixed-strip literal vacuum inclusion/compression and
coarse form; additive independent/common-Gauss extensions with complete low
spaces; a generic intrinsic-score criterion whose uniform Wilson premises
remain open; and an entire Gaussian quantum complement/source theorem for
the specified weighted coordinate space. The last theorem has an all-size
harmonic application with an explicit regulator and flat-sector limitation.
No source family is identified with all OS histories merely by this package.

No new Lean theorem is presently part of this successor. Do not import
earlier strict compiler success as certification of the new general claims.

## 2. Original evidence inventory

`inventory_originals.py` records exact current bytes in
`CURRENT_ORIGINAL_INPUTS.json`. It distinguishes required evidence not yet
written from optional provenance, and checks declared source hashes in each
available original report. This is a planning snapshot, not a run seal.

Required original control pairs:

1. `next_literal/check_literal_vacuum_projection.py` and its JSON.
2. `next_literal_common/check_common_gauss_literal_projection.py` and its JSON.
3. `next_literal/score_controls/check_ground_marginal_score.py` and
   `ground_marginal_score_controls_final.json`.
4. `next_gaussian_full/check_entire_gaussian_literal_complement.py` and its JSON.
5. `next_quantum_score_center/check_central_score_identity_independent.py` and
   `central_score_identity_independent.json`.
6. `next_literal_inverse/check_literal_inverse_energy.py` and
   `literal_inverse_energy_controls.json`.

Required reviews/provenance include the five original proofs and inverse addendum,
`next_literal_common/ROOT_COMMON_GAUSS_AUDIT.md`,
`next_literal/score_controls/INDEPENDENT_SCORE_AND_LITERAL_AUDIT.md`, and
`next_gaussian_full/INDEPENDENT_GAUSSIAN_FULL_AUDIT.md`, and
`next_quantum_score_center/INDEPENDENT_CENTRAL_SCORE_AUDIT.md`. Include additional
source-owner/root final audits once explicitly provided and frozen.

The score control's unsuffixed `ground_marginal_score_controls.json` is a
draft from before its final source's structural sorting change. It must not
be advertised or copied as the current source-matched certificate. Preserve
that existing draft in outputs; the final report is the acceptance input.

The literal control resolves repository predecessors through the original
directory depth. Preserve an `original_tree/` miniature tree containing its
exact repository-relative script/proof paths and every predecessor named in
its `sources` map. Do not patch the original program to use the new run root.
Top-level documentary copies may be provided for canonical links, but the
README must give the executable original-tree path explicitly. The other
original controls need their sibling proof files or exact declared paths;
inspect each final CLI before choosing the replay working directory.

## 3. Freeze gates before assembly

Do not create the canonical run until all of the following are available:

1. Root authorization and successor branch/run name are recorded above;
   no additional consent gate is needed.
2. Documentation owner hands off five final canonical note hashes and their
   expected run-link inventory. Root supplies the five unique paper pins.
3. Native-code owner hands off the final module/invariant/test/runner hashes,
   actual check names/IDs, schema and final runner SOURCES tuple. No guessed
   counts or IDs enter the manifest or ledger.
4. All six original program reports match their actual source snapshots.
   A changed proof or program must have an intentional fresh certificate,
   retaining earlier evidence with its proper historical scope.
5. The complete mathematical audit and applicable control results are frozen.

Canonical changes remain owned by root/documentation/native agents. The run
assembler owns only the new run and its assembly outputs. Earlier sealed
runs must remain byte-for-byte unchanged.

## 4. Proposed run layout

- `certificate.json`: fresh native certificate from the final guarded runner.
- `native_runner.log`: actual command output, with runtime/environment scope.
- `source/`: every final native SOURCES path, mirrored byte-for-byte; include
  analytic predecessor files needed for portable proof provenance as separate
  explicit additions, not undeclared executable dependencies.
- Top-level original control/proof/audit copies with their existing basenames
  wherever canonical links expect them.
- `original_tree/`: the unchanged original relative execution layout and
  exact source versions named by original reports.
- `environment/pyproject.toml`, `environment/uv.lock`: declared dependency
  provenance for the selected source tree.
- `provenance/`: final proof/native freeze handoffs, copy/source integrity
  inventories and mathematical audit scope.
- `replay_frozen.py`, read-only replay log, negative-control report,
  cold-relocation report, link-validation report and clear README.
- `SHA256SUMS`, written only after all mutations are complete, excluding
  itself and covering exactly every other file. No caches or `.pyc` files.

The README distinguishes full analytic conclusions from the finite examples:
nonuniform true marginal, exact-vacuum tensor leakage, common-Gauss singlets,
nonzero connection/Fisher factors, full Gaussian number bounds, and the
physical wrong-source quotient. A finite occupation cutoff does not certify
the entire Fock theorem; that conclusion comes from the analytic number-form
argument. A positive regulator does not supply an unregulated torus vacuum.

## 5. Execution and acceptance sequence

1. Snapshot hashes of all original inputs and current canonical/native
   sources. Require a new destination and fresh certificate paths.
2. Run the final native verifier with assertions enabled. Copy every pinned
   source and preserve every original program/report without alteration.
3. Replay native modules through a fake package with empty package paths,
   loading only the pinned source files. Compare complete recomputed control
   payloads and native check records to the frozen certificate.
4. Replay each original control from its copied original layout. Compare its
   exact mathematical payload and every declared source hash. Ignore only
   fields explicitly declared runtime metadata. Reject undeclared scientific
   dependencies by blocking NumPy/SciPy; use declared SymPy/stdlib only.
5. Test actual failure boundaries: invalid positivity/hypotheses; broken
   vacuum inclusion; wrong source frequency weight; missing connection
   divergence or density term; incomplete low spectrum; corrupted exact
   certificate; optimized Python; and overwrite attempts. Keep the native
   check's mathematical scope narrower than its analytic proof when needed.
6. Verify source hashes are unchanged. Write manifests, then test deliberate
   corruption on a disposable copy, never the sealed source run.
7. Copy the entire proposed run outside the checkout and replay with an
   isolated interpreter, no PYTHONPATH/PYTHONHOME, no bytecode writes and an
   audit denying original-checkout source reads while allowing declared
   interpreter dependencies. Run a fresh relocated native certificate and
   compare all non-runtime fields. This tests relocation, not a new package
   installation; report that boundary honestly.
8. Validate all expected canonical run links, all manifest hashes and the
   complete file set before and after replay. Record final counts/hashes.
9. Send root the final manifest digest, certificate schema/counts/source pins,
   replay/negative/cold/link outcomes and exact scope. Root registers the
   run and scoped supports, regenerates views and runs final project gates.

This plan has no permission to revise a frozen proof to make a computation
pass. A concrete failure must be isolated and reported to the source owner;
any source revision requires a fresh handoff and intentional reseal.
