# Remark 9.Y (relation to Prop. 9.6).

Remark 9.Y (relation to Prop. 9.6).
Proposition 9.6 (Combes–Thomas) requires only “finite range + uniform positivity” and gives \eta_{\mathrm{CT}}\sim m^2/(\alpha D_E). Proposition 9.X uses additional Laplacian/Dirichlet‑form structure of d_1^*d_1 (via the semigroup and a Davies perturbation bound) and improves the small‑mass scaling to \eta_{\mathrm{DG}}\sim m/\sqrt{\alpha D_E}. As in Remark 9.7, for the clustering program it is enough to have some volume‑uniform \eta>0; the present proposition is only needed if one wants a decay rate closer to the physically correct correlation length scaling.

⸻

If you want, I can also tailor the constant D_E in (9.DG.2) to the actual off‑diagonal row‑sum constant for \Delta_1=d_1^*d_1 in your normalization (your diagnostics show a smaller “C_0” than the crude degree bound), which improves the numerical exponent without changing the proof structure.
