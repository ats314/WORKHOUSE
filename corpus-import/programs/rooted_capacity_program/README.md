# rooted_capacity_program — doc layer uploaded by Alex June 12, 2026 (F025; deep read = ledger #47b)

**Definition of done:** the display-math re-export blocker is resolved, the trio is deep-read (#47b) and slotted into the status map, and each result is either folded into the pmbsf/op1 deliverables or explicitly retired. This is an input/doc layer — nothing here ships as a standalone claim.

Three documents (.docx originals + pandoc .md conversions; MD5s below). **Recovered-but-unreviewed; statuses below are the documents' OWN words, not adjudication.**

| MD5 | Doc | What it is (self-description) |
|---|---|---|
| cd9a94b0… | Logical Status and Theorem Map | Three-layer map: (I) local SU(3) one-plaquette class spectrum (rank-two Weyl correction); (II) finite-dimensional Wick certificates for fixed-rank SU(N) coefficients; (III) **conditional** projected-capacity route for global Wilson defect geometry. Verbatim honesty frame: "The results are **not presented as a proof** of the four-dimensional Yang–Mills mass gap… proven statements are local or finite-dimensional unless explicitly marked"; organizing principle "local class asymptotics ⟹ finite algebraic certificates ⟹ conditional global mechanism." |
| cf3c7792… | Rooted Projected-Capacity Source Stability | The deterministic projected-capacity layer + conditional hard-defect Peierls estimate ⟹ **rooted source-stability criterion**, "conditional on the Wilson free-energy stability input and on a deterministic projected-capacity envelope." Central object: rooted capacity of the connected defect island at a root plaquette (explicitly rejects the global top norm at fixed defect density). |
| 688fe72e… | SU(3) Three-Term Local Gap | Local one-plaquette class-sector gap through O(β^(−1/2)); finite computation in ℝ[p₂,p₃]^{S₃} through degree 6 with the Weyl–Gaussian inner product (discriminant weight ½p₂³ − 3p₃²). |

**Corpus connections (identification level):** Layer III + the rooted theorem sit in the PMBSF/OP-1 orbit — the rooted-capacity object is what `rooted_capacity_peierls_path.ipynb` and the wilson_su3 rooted-probe notebooks compute (F015/F019 "OP-1 feeders"), and "source stability" is the M3a/Theorem-Z.B vocabulary (`op1_defect_sparsity/PLAN_OP1_unif_closure.md`); the projected-capacity layer continues `13_PMBSF` (rank_two_weyl_projected_capacity_final.tex, A100 capacity runs). Layers I–II + the three-term gap connect to the one-plaquette program; the Weyl-discriminant computations in C:\SIMULATIONS notebooks (Untitled178/45, GOODRESULTS1) are their authoring traces. **Whether the rooted source-stability criterion advances (S)/M3a is Alex's determination** — the docs themselves label the route conditional.

**Appended June 12, 2026 (evening pass — re-export check):** Alex's fresh 16:52 download of all three docx (Downloads; "Logical Status" ×5 copies) is **byte-identical to the May-30 exports** (MD5 match: cd9a94b0…/cf3c7792…/688fe72e… — same files as this directory). Consequence: standing note (i) below is **still unmet** — the export pipeline reproduces the math-stripped file; a re-download cannot fix it. Recovery needs a different route from the authoring source (PDF export, or paste the display math as text/LaTeX). Also verified: the unsuffixed May-30 "Logical Status and Theorem Map.docx" (5ac60729…, in no store) differs from the archived (1) edition **only in docProps/core.xml metadata** — document.xml byte-identical ⇒ single content edition exists; nothing missing.

Provenance: session uploads June 12, 2026 (filenames carried "(1)" export suffixes); secured same-session to this directory. **Deep read done (F026)** — theorem-by-theorem status table in `records/review/findings/F026_rooted_capacity_deep_read.md`. Three standing notes: (i) **the Rooted doc's display math is empty in the uploaded source** (chat→docx export stripped it; raw extraction = `NOTE_RCAP_rooted_raw_extract.txt`) — re-export requested from Alex; the Map's Layer III carries the schematic content meanwhile. (ii) **Erratum recorded by the Map:** odd-sector constant is −3(N²−3)/16N; the older −3(2N²+1)/16N "does not belong to the final theorem stack" — quoting hazard in era documents; the deposited `ENGINE_SUN_codd_local_gap_exact.py` certifies the corrected value (concordance verified F026 §3). (iii) The trio's single **Open input** ("inhomogeneous Wilson free-energy stability") is the Z.B/far-source-stability class of the M3a pair; Z.A (LCI typicality) is not addressed here.
