# m4_scaling — M4 physical-units scaling tables (precomputed June 12, 2026)

`ENGINE_OP1_m4_scaling_tables.py` → `CERT_OP1_m4_scaling_tables.json`; interpretation + caveats: `NOTE_OP1_m4_scaling_2026-06-12.md`. The tables are exact finite computations; their *consumers* (the conditional closure note, milestone M4 of `programs/op1_defect_sparsity/PLAN_OP1_unif_closure.md`) remain blocked on M3 = the open sparsity lemma (S). Precomputation ≠ closure.

**Evening pass June 12:** the note's open diagnostic is resolved by `ENGINE_OP1_m4_harmonic_decomposition.py` → `CERT_OP1_m4_harmonic_decomposition.json` + `NOTE_OP1_m4_harmonic_decomp_2026-06-12.md` (17 rows incl. L=12; gates G1–G4 + cold-rerun reproduction). Headline: the scaling instability is exactly the 4-mode harmonic sector (closed form T_H = 1/(m₀⁴L⁴), constant on the physical diagonal); the coexact remainder is stable (×1.63 band); the rank-4-exact split certificate holds N\*_split ≈ 30 flat where plain N\* erodes to 21. Caveat: `CERT_OP1_m4_scaling_tables.json` is missing its s=1.0/1.25 rows (engine overwrite defect, documented in the DECOMP note §Run record) — the decomposition json is the authoritative M4 data file. M3-blocked status unchanged.

**Late pass June 12 (first under DECISIONS #009 — see `NOTE_OP1_m4_tc_asymptotics_2026-06-12.md`):** T_C has an exact momentum-space closed form, T_C = (3/4)(1/Ns)Σ_{k≠0}(m₀²+α_Wλ(k))⁻² — `ENGINE_OP1_m4_tc_fourier.py` (gates F1–F5: reproduces kernel_consts at 2e-15 and all 17 DECOMP rows at ≤1.5e-5 — since identified as a β-rounding generation mix in the DECOMP json, not CG noise: F027) + `ENGINE_OP1_m4_tc_realspace_check.py` (independent CG cross-check at L=16/24, dev ≤7e-15; `opcache/` is its regenerable ~73 MB operator cache, safe to delete). Diagonal extended to s=128 (L=512): **no saturation — α²T_C = (3/64π²)(2 ln s + ln α + C), slope confirmed to 0.5%**; N\*_split erodes logarithmically 30→16. `ENGINE_OP1_m4_sparsity_interface.py` (gates D1–D4) then closes the loop with the stored OP-12 ensembles: closure condition **q + c_af·κ > 2** (v₀ ~ a^q); measured κ̂ = 1.24–2.57 vs 28.71 required at q=0 (structurally capped at O(2–4)) ⇒ **q=0 dead, q=2 closes for any exponential sparsity, breakeven q\* ≈ 1.82–1.91** — the v₀-scaling sign-off is load-bearing for M4. The fourier json is now the authoritative scaling data (supersedes the DECOMP json's diagonal, whose ≤1.5e-5 offsets are a β-rounding generation mix — F027). Note assembly still awaits M3 + the v₀ decision.

**F027 (same day):** Alex's uploaded parallel-session trio `m4_tc_closed_form.{py,json}` + `NOTE_OP1_m4_tc_closed_form_2026-06-12.md` (verbatim, md5s in MANIFEST) independently re-derives the same closed forms and adds: citable Theorem-1 proof text, the g_C/off-diagonal closed forms, a **PROVED Tier-1 uniform T_C bound**, a **Tier-2 uniform ceiling (N\*_C ≥ 8, N\* ≥ 7 for all s)** — confirmed here by direct ridge evaluation (T_C^∞ max = 0.11692 at s ≈ 3×10¹⁵) — and the β-rounding forensics that corrected this directory's noise attribution. Reviewed CONCORDANT at 3e-15: `records/review/findings/F027_closed_form_trio_review.md`. Its engine expects `/mnt/project` + `/home/claude` paths — run via adapted copy (F027 §2).

**F028 (same day):** Alex's uploaded `NB_OP1_m5_tier_2_ceiling_pin_auditor.ipynb` (verbatim; "M5" is a misnomer — this is M4 hardening) reduces the Tier-2 pins to one scalar inequality with exact threshold BCRIT = 57/1210 (verified in rationals). Audit engine `ENGINE_OP1_m4_ceiling_pin_audit.py` (gates P1–P5): the notebook's all-L quantifier is unprovable (B(x) crosses BCRIT at ln s ≈ 9819 — D(x) = Y−Ax−(A/2)lnα is constant = 0.021301939 to 3.8e-14), repaired by (A) finite window + Tier-1 tail (13% splice margin) or (B) the recommended three-parameter majorant with D_crit = 0.044752 — **uniform ×2.10 headroom**. The remaining analytic task for the uniform floors N\*_C ≥ 8 / N\* ≥ 7 is a scalar heat-kernel/theta-tail bound of form (B). `records/review/findings/F028_ceiling_pin_auditor_review.md`.

**F030 (same day, late evening):** Alex's M5.1 + M5.3 notebooks (verbatim; em-dash/+ filenames ASCII-normalized; md5 664e657c / 089f6061). **M5.3 splits the F028 pin into two referee-ready lemmas:** Lemma A (cover domination Φ_L⁴ − L⁻⁴ ≤ q⁴) + Lemma B (infinite-lattice constant C_inf ≤ 0.013 vs C_crit = 0.0362419 = D_crit − (A/2)ln6, concordant 1.4e-12). M5.1 = integer-scan finite certificate L = 4..4096 (uniform margin ≥ 0.0286 to 57/1210; notebook ships no asserts — gated here). Audits: `ENGINE_OP1_m4_bessel_scan_audit.py` (PB1–PB7; resumable; state json deposited, delete it to force the full ~90 s rescan) + `ENGINE_OP1_m4_cover_domination_audit.py` (Q1–Q7) — ALL GATES PASS. New constants: **D∞ = 0.021301940188 as a renormalized integral (T0-invariant 2e-17, authoritative digits)**; C_inf = g(μ²) one-variable form, g′ < 0 gated; **finite-D ridge D_L max = 0.0182889 at L = 47, strictly under the D∞ envelope (×2.45 finite headroom)** — on the diagonal μ²L² = 48/β = O(1), so D_L never approaches D∞. Extender caveats (guarded in the audits): scipy `ive(0, z)` nans for z ∈ (2e8, 2e10]; QUADPACK semi-infinite probes overflow t². Review: `records/review/findings/F030_m5_bessel_cover_domination_review.md`.

**F031 (same day, late night) — LEMMA B PROVED:** `ENGINE_OP1_lemma_b_cert.py` + `LEM_OP1_lemma_b_cert.json` (gates **LB0–LB9 ALL PASS**, in-store cold run 0.7 s + 1.9 s; dependency: mpmath only; modes `cert` = pure-rational certificate, `float` = validation, `all`): **g(μ²) ≤ G_BOUND = 0.018664535031 < 1/28 for all μ² ∈ (0, 15/28] ⇒ C_inf(x) < 1/28 ∀x ≥ 0** — exact rational arithmetic end-to-end (Fractions, directed rounding; π/γ_E cited intervals; ln 6 by atanh-series brackets); the analytic input is the elementary upper bound r(t) ≤ 1 + 1/(16t) + (2311/10⁴)/t² (t ≥ 4) proved in the note. Proof: `programs/op1_defect_sparsity/LEM_OP1_lemma_b_proof_2026-06-12.md`. **Floors N\*_C ≥ 8 / N\* ≥ 7 now conditional on Lemma A alone.** Also deposited (session-P batch, verbatim, F031 §2 md5 table): `ENGINE_OP1_m5_audit.py/.json` (G-M1–G-M8), `ENGINE_OP1_m5b_region.py/.json` (G-A1–G-A6), `NB_OP1_m5_4_warning_free_limit_constant_audit.ipynb` (blocks M5.4–M5.8). **Cold-run record:** both engines re-run on our stores via path-shimmed copies (sed `/home/claude/` → run dir; `ENGINE_OP1_m4_tc_closed_form.py` importable from this directory; m5b reads m5_audit's json for G-A5): ALL GATES PASS, output jsons reproduce the uploads to ≤ 1 ULP (worst 1.1e-16, one diagnostic). Their companion notes + master docx: `programs/op1_defect_sparsity/`. The session-P **Lemma-A proof claims are not yet independently reviewed** (ledger #53). Review: `records/review/findings/F031_lemma_b_proof_and_m5_batch_intake.md`.

**F032 (same day, directly after):** #53 executed — **all three session-P Lemma-A proofs REVIEWED-PROVED** (Claim 1; continuum core both regions; Region-A theorem). `ENGINE_OP1_lemma_review_cert.py` + `.json` (gates RA1–RA6 ALL PASS in-store, 3 s; md5 4c590afd): re-pins every constant the proofs use, documents 4 benign nits (true region-(a) sup = G(1/8) = 0.145055, not the printed 0.1381 — proved 0.1482 covers it), and independently reproduces M5.8's compact-window margin (max E = −0.039584) and M5.6b's box value by a coarse directed method. Lemma A's open content after this: **compact window τ ∈ (0, 0.4] only.** `records/review/findings/F032_session_p_lemma_a_proofs_review.md`.

**F033 (same day, directly after) — LEMMA A PROVED, FLOORS UNCONDITIONAL:** `ENGINE_OP1_lemma_window_cert.py` + `.json` (gates **WB0–WB5 ALL PASS** in-store, 7.4 s; md5 69aa61e6/cbc49a80; resumable; mpmath dps 25 + directed 1e-18 padding vs margins ≥ 0.045): compact window τ ∈ (0, 0.31] certified (uniform L ≥ 12 by Λ\*-monotonicity, one 560-cell sweep; per-L 4..11 × 760 cells; min{exact-difference, convexity} form; head-kills; small-τ dyadic + τ¹⁰ tail; Lemma W contour-shift wrap bound validated; R_MAX = 1.19, 600 cells) + Region-A extension to τ₀ = 0.31. **⟹ N\*_C ≥ 8 / N\* ≥ 7 unconditional on the diagonal** (chain: F031 + F032 + F033). Proof note: `programs/op1_defect_sparsity/LEM_OP1_lemma_closure_2026-06-12.md`. Erratum: true sup r = 1.17511 @ t ≈ 0.40.
