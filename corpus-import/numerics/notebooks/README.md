# numerics/notebooks/

Jupyter/Colab notebooks not tied to one campaign. Campaign notebooks live under `programs/`.

Broadly: the A100 SU(3) combined `T1pm`/OP12/FFT and string-tension pilots; the SU(3) Y5/Y6 historical recovery and `m6` audit chain; SU(4)/SU(6) determinant and exceptional-enumerator notebooks; the prism/prismatic falsification and escape tests; the `op12_a100` screens; the rooted-capacity Peierls path; and the `y4_*` from-scratch and certificate notebooks.

Two cautions:

- **Notebook outputs are not certificates.** A stored cell output is `Record-backed` at best on the corpus evidence scale — below a cold reproduction. Promote a result only when a gate reproduces it from source.
- Several notebooks here are near-copies of `programs/` material that differed by at least one byte, so global deduplication left both. When in doubt about which is canonical, check `records/REORG_MANIFEST_2026-08-20.tsv`.
