# records — the running log + the review campaign

`SESSION_LOG.md` — **the one running log** (append an entry at the end of every pass; CLAUDE.md rule 7). Negative results and failed routes are recorded here, not buried. `review/` — the corpus review campaign (protocol, ledger, findings). Nothing in records/ is status-bearing on its own: live status lives in `STATE.md`, live problems in `theory/DOC_GOV_open_problems.md`.
