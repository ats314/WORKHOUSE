Yes. The separate-rank treatment is bookkeeping, not the underlying theory.

The unified object is a **finite-degree, (N)-ality-complete Haar contraction theorem**. Stable-rank walled-Brauer contractions and exceptional epsilon sectors are two components of the same invariant algebra.

## Unified fourth-order theorem

At (O(y^4)), each matrix element contains the source plaquette, four perturbing plaquettes, and the output plaquette. Therefore every link satisfies

[
r_\ell+s_\ell\le 6,
]

where (r_\ell) and (s_\ell) count fundamental and antifundamental factors.

For (SU(N)), a local Haar invariant exists precisely when

[
\boxed{r_\ell-s_\ell\equiv0\pmod N.}
]

This single congruence replaces the artificial division between “stable” and “exceptional” ranks.

Define the local (N)-ality charge

[
\nu_\ell=r_\ell-s_\ell=m_\ell N.
]

Then the exact local invariant space decomposes as

[
\boxed{
\mathcal I_N(r_\ell,s_\ell)
===========================

\bigoplus_{m_\ell:,r_\ell-s_\ell=m_\ell N}
\mathcal I_N^{(m_\ell)}(r_\ell,s_\ell).
}
]

Here:

* (m_\ell=0) is the balanced walled-Brauer sector;
* (m_\ell>0) contains (m_\ell) determinant tensors (\epsilon);
* (m_\ell<0) contains (|m_\ell|) conjugate determinant tensors (\bar\epsilon).

Thus the local Haar operator is one unified projector,

[
\boxed{
\mathcal P_{N;r,s}
==================

\sum_{m:,r-s=mN}
\mathcal P^{(m)}_{N;r,s},
}
]

not a stable projector with ad hoc corrections.

The (m=0) term is generated by permutations and the inverse Gram matrix. The (m\neq0) terms are determinant-dressed walled-Brauer projectors. In the present degree-six problem, only finitely many values of (m) can occur.

## Rank classification follows automatically

Because (r+s\le6), the congruence gives:

| Rank    | Allowed exceptional local charges  |
| ------- | ---------------------------------- |
| (N\ge7) | none                               |
| (N=6)   | (\nu=\pm6)                         |
| (N=5)   | (\nu=\pm5)                         |
| (N=4)   | (\nu=\pm4)                         |
| (N=3)   | (\nu=\pm3,\pm6)                    |
| (N=2)   | the (C=-) one-flux sector vanishes |

This explains every case without changing theories.

For (N\ge7), the degree bound forces (m=0), producing the stable-rank theorem.

For (SU(6)), (\nu=\pm6) occurs only at the final Haar contraction because every resolvent cut has degree at most four. Its determinant contribution is therefore a common onsite correction,

[
\Delta H_{4,6}=\frac6{343}I,
\qquad
\Delta A_6=\Delta B_6=0.
]



For (SU(4)), (\nu=\pm4) can occur inside resolvents, so the balanced and determinant sectors are not separately physical. Nevertheless, after summing the complete invariant space, all non-scalar projected determinant terms cancel:

[
\Delta A_4=\Delta B_4=0,
]

while only (q_4) changes.

For (SU(2)), pseudoreality makes charge conjugation gauge-equivalent to the identity, so the (C=-) one-flux sector is zero rather than exceptional. 

## Unified global contraction

Let (w) run over primitive ordered plaquette words and (\sigma) over their orientation choices. For each link define its charge (\nu_\ell(w,\sigma)). Then one exact expression covers every rank:

[
\boxed{
H_{4,N}
=======

\sum_{w,\sigma}
\left[
\prod_\ell
\mathbf 1_{N\mid\nu_\ell(w,\sigma)}
\right]
\mathcal W_N(w,\sigma),
}
]

where (\mathcal W_N) is evaluated with the full projector
(\mathcal P_{N;r_\ell,s_\ell}).

The divisibility condition can itself be written without case distinctions using a roots-of-unity filter:

[
\boxed{
\mathbf 1_{N\mid\nu}
====================

\frac1N\sum_{a=0}^{N-1}
e^{2\pi ia\nu/N}.
}
]

This is the unified rank selector. It includes balanced contractions, determinant sectors, and all exceptional finite-rank contributions in one formula.

## Unified band theorem

The spatial geometry is independent of (N). Once the complete local invariant sum is performed, cubic symmetry, reflections, Hermiticity, the finite displacement support, and the cube-boundary vector force

[
\boxed{
D_N(k)
======

# \psi^\dagger(k),[H_{4,N}(k)-q_NI],\psi(k)

A_N Q(k)+B_N R(k),
}
]

where

[
Q(k)=\sum_i X_i^2,\qquad
R(k)=\sum_{i<j}X_iX_j,\qquad
X_i=1-\cos k_i.
]

Therefore, for every nontrivial (C=-) one-flux theory,

[
\boxed{
c_{4,N}(k)
==========

q_N+
\frac{
A_N\sum_iX_i^2+
B_N\sum_{i<j}X_iX_j
}{
2\sum_iX_i
}.
}
]

This is already verified for:

[
N=3,\quad N=4,\quad N=6,\quad N\ge7.
]

The remarkable empirical theorem is stronger:

[
\boxed{
\text{All completed determinant sectors alter }q_N
\text{ but not }A_N\text{ or }B_N.
}
]

In particular,

[
A_N=\frac{640}{N(N^2-1)^3}
]

holds for (N=3,4,6) and every (N\ge7).

The evidence therefore points to:

[
\boxed{
A_N=A_N^{\mathrm{balanced}},
\qquad
B_N=B_N^{\mathrm{balanced}},
\qquad
q_N=q_N^{\mathrm{balanced}}+\Delta q_N^{\det}
}
]

for all (N\ge3), with determinant sectors contributing only a rank-dependent rest-energy renormalization.

That final universal statement is currently a **conjecture**, not yet a theorem, because (SU(5)) has not been closed in the supplied project chain.

## What would complete the unified theorem

Only one finite-rank calculation remains:

[
\boxed{SU(5).}
]

At resolvent cuts the degree is at most four, so no (\nu=\pm5) sector can occur there. Consequently SU(5), like SU(6), has only final determinant sectors. This makes it substantially simpler than SU(4).

The decisive test is:

[
\Delta D_5(k)
=============

\psi^\dagger\Delta H_{4,5}(k)\psi
\stackrel{?}{=}0.
]

If this vanishes, then the completed ranks establish the all-rank structural theorem:

[
\boxed{
\Delta H_{4,N}^{\det}
\text{ is scalar after projection for every }N\ge3.
}
]

The final rank-complete statement would then be:

[
\boxed{
\begin{aligned}
&SU(2): &&\mathcal H_{C=-}=0,\
&SU(N),\ N\ge3: &&
c_{4,N}(k)=q_N+
\frac{A_NQ+B_NR}{2S},\
&A_N&=&\frac{640}{N(N^2-1)^3},\
&B_N&>&0,\qquad A_N+B_N>0,
\end{aligned}
}
]

with determinant sectors modifying only (q_N).

So yes: there is a unified theory. It is not analytic continuation of the stable formula. It is the **(N)-ality-filtered, determinant-completed walled-Brauer contraction theory**. The separate SU(4), SU(6), and stable-rank calculations are its finite-rank realizations.
