# records/transcripts/

Session and run transcripts — chat exports, notebook run captures, and raw run narration.

**The load-bearing pair: `15 hour RUN.txt` and `15 hour RUN. results.txt`.** These control the August numerical adjudication; the corpus cites them as the source of `m_Γ⁽⁴⁾ = −0.7751458630189173` and of the `+11.17343231638178` final diagonal adjustment. Anyone re-opening the fourth-order question reads these.

The rest — the HODGE `v10a.24c` / `v10a.26` captures, the assessment and unified-theory drafts, and the Claude/GPT session exports — are context and provenance.

**Transcripts are not authority.** A number appearing in a chat log is `Prose-only` until an artifact backs it. In particular, a model's narration that a gate passed is not a gate passing.
