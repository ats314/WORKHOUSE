# Single implementation-planning handoff

There is one handoff because there is one system and one ordered path. Do not split this prompt into competing implementations or parallel production workstreams.

```text
/make-plan

Plan one canonical GLUEBALL SU(3), L=5, O(u^4) computational system with one production entry point:

    python -m glueball_o4.runner run --fresh

Read first:
- PATHFINDER-2026-08-19/00-features.md
- every file in PATHFINDER-2026-08-19/01-flowcharts/
- PATHFINDER-2026-08-19/02-duplication-report.md
- PATHFINDER-2026-08-19/03-unified-proposal.md
- PATHFINDER-2026-08-19/05-latest-run-forensics.md
- PATHFINDER-2026-08-19/06-pattern-2-5-runtime-forensics.md
- PATHFINDER-2026-08-19/07-repeat-v25-delivery-forensics.md
- the v1.4 registry and SU3 closure checklist inside sources/PAPER_FLUX_gauge_constrained_spectral_geometry_unified_v1_4_2026-08-08.zip

The algorithm and sequence are already decided. Do not offer options or branches:

1. lock one authority/specification snapshot;
2. replay accepted lower-order fixtures from a fresh process;
3. derive and certify source-y to canonical-u normalization;
4. build exact P;
5. build exact physical Q1 with the typed key `(canonical joint irrep, exact H0 energy, canonical center flux)` and `B B^T=K` witness;
6. build exact physical Q2 with `B12`, E0-pole, and actual local-occurrence-census gates;
7. use one exact factorized Haar contractor covering precisely the canonical O4 patterns; enforce `p+q<=6`, never build `W22`/`Q2->Q2`, and treat `(2,5)/(5,2)` as a fatal upstream-order violation with full provenance;
8. compute exact physical PVP and the one canonical Hermitian SW/BCH fold;
9. enumerate and evaluate exactly the canonical 3,895 Stage-3H topology records, including the formerly missing 2,417 folded cases;
10. assemble one unshifted 189-record real-space H4 kernel;
11. run outward interval and uniform Gamma-touching proofs on that kernel hash;
12. seal the full pre-unblind manifest;
13. load Hamer/historical constants exactly once for comparison;
14. promote one claim and generate all documentation only if every preceding gate passed.

Implementation baseline and narrow migrations:
- start from the executed v10a2 17/17 physical-Q frontier;
- repair the v10a3 producer/consumer key mismatch;
- extract only the physical-PVP and multidimensional fold concepts from v10a24c;
- rewrite useful v10a20b denominator intent so arithmetic is exact at origin;
- migrate no v10a26 Factor52 code: it supports the fifth-order `W22` workload accidentally introduced by v25, not the selected O4 path.

Explicitly retire from production:
- all v10a20-v10a26 notebooks as runners;
- finite-cluster, rooted-oracle, Gelfand, polynomial-fit, and dual-cold result paths;
- every `(2,5)/(5,2)` contractor and every `W22`/`Q2->Q2` block at O4;
- F07 literal FOLD_EX injection;
- F08 as an oracle;
- any scalar shift chosen to match an oracle or historical target;
- algorithm feature flags or factories;
- partial/smoke promotion;
- live-kernel or weak-signature resume;
- Monte Carlo as part of this fixed-order plan.

Plan one append-only artifact chain. Every stage must include source/spec/environment/input/output hashes, exact parent ID, arithmetic status, required gates, payload hash, timing/resources, and terminal status. The gate primitive must raise immediately on fail, skip, NaN, missing output, cardinality mismatch, or hash mismatch. Final promotion requires one fresh end-to-end run; resumptions are diagnostic only.

Deliver a phased implementation plan in the exact sequence above, with concrete destination files, exact old call sites to extract or delete, characterization fixtures, acceptance tests, stage schemas, migration checkpoints, and rollback points. At every milestone state the strongest claim permitted. Do not introduce a second production entry point, second algorithm, reconciliation step, alternate kernel, or parallel lane.
```
