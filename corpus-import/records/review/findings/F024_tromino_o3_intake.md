# F024 — C:\SIMULATIONS addendum: Alex added the engine corpus; tromino O(y³) suite intaken + cold-run (all gates pass)

**Date:** 2026-06-12 · **Trigger:** 14 `.py` files added to C:\SIMULATIONS after the F023 survey (store now 285 files; manifest v2 frozen).

1. **Identity:** 5 of 14 are byte-identical to archived copies (cls_flat_band_certificate — **so CLS v1.1 stays lost**; su3_moments_ext; su3_domino_d3; glueball_band_certificate_v2; master_v2_regression_certificate). **9 genuinely new:** the 8-file **tromino O(y³) suite** + ENGINE_FLUX_glueball_band_certificate.py (v1 predecessor).
2. **Tromino suite = the next-order flat-band program** (does the C-odd flat band survive two-hop geometries at O(y³)?). All engines cold-run in THEORY with all gates passing: cards 17, constraint 15, closed-form (exact α(k), width 64/27), primitives 25 (exact Weingarten/ε/Fierz layer), extractor v1_1 12/12, independent symbolic check 16. Two Colab/agent path relics handled without touching the verbatim deposits (`/mnt/data/`, `/home/claude/review/` — the latter confirming agent-session authoring).
3. **Two-level conclusion recorded (not adjudicated):** candidate diagnostics — the Untitled221 primitive weights (2/9, 2/9, 2/27) would lift the band (both scripts disclaim those weights as physical); independent symbolic check — σ-covariance + purely-ε-mediated NN content force H_eff = αI + bS ⇒ "flat band stays EXACTLY FLAT at O(y³)" (consistent with the June-11 d₃ 251/251 result; implies the candidate weights are non-physical). The physical SU(3) resolvent/channel weight computation is the suite's own stated open step.
4. **Deposits:** `programs/one_plaquette/tromino_o3/` (8 engines + README with run log + caveats), `programs/one_plaquette/ENGINE_FLUX_glueball_band_certificate.py` (v1), manifest v2 (285), SOURCES/STATE/ledger #46/SESSION_LOG; flat-band paper TODO pointer (the suite bears on the paper's next-order roadmap). Archive intake of the 9 new engines into `E:\YANG ORGANIZED/12_ONE_PLAQUETTE/` recommended to Alex (E: is read-only to agents absent direction).
