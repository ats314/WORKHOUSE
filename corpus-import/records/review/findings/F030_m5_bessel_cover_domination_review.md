# F030 — Review of M5.1 (Bessel finite certificate) + M5.3 (cover-domination closure route) (uploaded June 12, late evening)

**Unit:** #51. **Verdict: M5.3 is the substantive advance — it splits the F028 scalar pin into two referee-ready lemmas (cover domination + an infinite-lattice constant), fully concordant with F028 (C_crit matches D_crit − (A/2)ln 6 to 1.4e-12); both lemmas audited and re-gated here, with the saturation constant now DERIVED as a renormalized lattice integral (T0-invariant to 2e-17), refining F028's D∞ digits. M5.1 is a finite certificate component: the integer scan L = 4..4096 verified end-to-end and hard-gated here (the notebook itself ships no asserts); its all-L pin statement re-states the two-parameter form F028 already proved unprovable-as-quantified — the scan is fine, the quantifier inherits the F028 repair. NEW structural fact found during gating: the finite-L constant D_L is NOT monotone — it peaks at L = 47 (D_ridge = 0.0182889) and drifts down; D∞ is a strict upper ENVELOPE, not a limit, because μ²L² = 48/β stays O(1) on the AF diagonal. Finite-scan headroom ×2.45.**

**Files:** notebooks verbatim → `numerics/op12_theta/m4_scaling/` `NB_OP1_m5_1_heat_kernel_bessel_finite_certificate.ipynb` (md5 664e657c..., original filename with em-dash/+ normalized to ASCII) and `NB_OP1_m5_3_cover_domination_closure_route.ipynb` (md5 089f6061...). Audit engines (this session): `ENGINE_OP1_m4_bessel_scan_audit.py` (gates PB1–PB7; resumable, state json deposited — delete it to force the ~90 s full rescan) and `ENGINE_OP1_m4_cover_domination_audit.py` (gates Q1–Q7); both ALL GATES PASS; jsons deposited. An M5.2 is referenced by M5.3 ("C_CRIT from M5.2") — by value-match (Q4) M5.2 ≡ the F028 auditor notebook; no separate M5.2 file exists in the batch.

## 1. M5.3 verified content [gates Q1–Q5]
Reduction: **Lemma A (cover domination)** Φ_L(t)⁴ − L⁻⁴ ≤ q(t)⁴ (q = e^{−2t}I₀(2t)) ⟹ C_L ≤ C_inf(μ²_L) pointwise (integrand-level, exact); **Lemma B** C_inf ≤ 0.013 < C_crit = 0.0362419 ⟹ T_C < 1/8, N*_C ≥ 8, T_full < 9/64, N* ≥ 7 for all L ≥ 4 — the logic checks. Q1: cover domination holds on the notebook's multiscale grids at 16 L values + 4× stress grids (worst gap −9.2e-19; equality approached only as t → ∞). Q2: all 22 C_inf rows reproduce (≤ 7.4e-10). Q3/Q4 (cross-normalization, the (A/2)ln 6 = 0.0085098 α↔β offset): C_sat + (A/2)ln6 − D∞(F028) = +1.19e-9 (within F028's quadrature budget — see §3); C_CRIT(M5.3) − [D_crit(F028) − (A/2)ln6] = −1.4e-12. Q5: envelope floors ≥ 8 on every safe row; the boundary row at C_crit prints floor 8 because T̄ = 0.125 lands exactly on a binary float — the mirror image of F028's floor-7 artifact (2-par form lands at 0.12500000000000003 → floor 7; PB7 reproduces that too). Three independent computations of the law constant now agree across two normalizations: F028 finite-x audit, M5.1's B_L → (A/2)ln α + D structure, M5.3's infinite cover.

## 2. New derived content [this review; gates Q6–Q7, PB6]
(i) **Identity:** C_inf(x) = g(μ²) with g(μ²) = Y_inf(μ²) − (A/2)ln(3/μ²), exactly (A·x + (A/2)ln β = (A/2)(ln(1/μ²) + ln 3) on the diagonal) — Lemma B is a ONE-VARIABLE statement about g on (0, 0.5357].
(ii) **Integral representation of the law constant (Q6):** C_sat = (3/4)[∫₀^{T0} t q⁴ + ∫_{T0}^∞ (t q⁴ − c/t)] − (A/2)(γ_E + ln T0 + ln 3), c = 1/(16π²) — T0-invariant to 2.1e-17 (T0 = 32/64/128), value 0.012792103146542; α-form **D∞ = 0.021301940188** (refines F028's measured 0.021301939001, whose 1.19e-9 deviation is inside its quad tolerance; these digits are now the authoritative ones). D∞ is thus a renormalized lattice integral — "measured machine constant" upgraded to derived-with-representation; the derivation is the classical E₁ expansion, machine-evaluated (float-grade, not interval-certified).
(iii) **Monotonicity (Q7):** g′(μ²) = (A/2)/μ² − (3/4)∫t²e^{−μ²t}q⁴ < 0 at all grid points (−2.6e-3 … −1.9e-2): sup C_inf = the μ→0 limit, numerically. 
(iv) **The finite-D ridge (PB6):** D_L rises to 0.018288870942 at **L = 47** (matching M5.3's own "finite corrected peak" grid comment) then drifts down (−3.7e-7/step at L≈150, −1.7e-8/step at 4096); D_L < D∞ for every scanned L — the cover-domination direction visible at the constant level; on the diagonal μ²L² = 48/β ∈ [7.3, 8.6] forever, so the finite lattice NEVER approaches the μ→0 envelope. Finite headroom D_crit/D_ridge = **2.4469** (envelope: 2.1008). My first gate here wrongly demanded D_L monotone ↑ (4049 violations) — wrong model, not noise; redesigned to gate the true shape.

## 3. M5.1 assessment [gates PB1–PB5, PB7]
Scan verified end-to-end: 18 probe rows ≤ 2.9e-11; dense closed-form cross-checks ≤ 1.4e-14 (and both sides match the notebook's printed pairs); B_L strictly increasing; max B_L = 0.0184734 at L = 4096, margin to 57/1210 = 0.028634; T_C < 1/8 throughout. Design notes: the notebook has **no hard gates** (prints booleans; the deposited audit supplies asserts); Y_upper adds |quad err| + a conservative spectral-gap tail envelope (upper-bias by design) but does not budget the Bessel-image truncation (≤ 1e-16 relative — harmless, noted for honesty); its closing "remaining lemma: B_L ≤ 57/1210 for all integer L ≥ 4" is the two-parameter form whose all-L quantifier F028 measured to fail at ln s ≈ 9819 — the scan (max x = 6.93) is consistent and useful, but the pin statement should be read through the F028 repair (form B / M5.3's split), not as stated. Environment caveats for extenders: scipy `ive(0, z)` returns nan for z ∈ (2e8, 2e10] and QUADPACK's semi-infinite transform probes t ~ 1e308 (overflow → inf·0 = nan); the audit engine guards both via the asymptotic branch t²q⁴ = c(1 + 1/(4t)) above t = 1e8.

## 4. Proof roadmap for the two lemmas [opinion + derivation sketches, per DECISIONS #009]
**Lemma A** ⟺ (Poisson summation) Σ_{m∈(Lℤ)⁴\{0}} p_t(m) ≤ L⁻⁴ ⟺ the zero-mode-deleted Riemann sum of e^{−tŵ} under-estimates its Brillouin integral. Equality holds as t → ∞ (Φ_L → 1/L), so any proof must be two-regime: t ≲ L² by Bessel image tails (I_L(2t)e^{−2t} ≤ e^{−L²/c t}-type), t ≳ L² by spectral collapse (only k = 0 survives, deficit e^{−tŵ₁(1)} vs q⁴ ~ t⁻² polynomial). Classical-flavored; the constants need care near t ~ L².
**Lemma B** needs NO monotonicity: two-sided error control on the E₁ decomposition gives g(μ²) ≤ g(0⁺) + R(μ²) with crude-constant R ≲ 0.014 ≪ margin 0.0234 (sketch: |∫₀^{T0}tq⁴(e^{−μ²t}−1)| ≤ μ²∫₀^{T0}t²q⁴ ≈ 0.0080 at T0 = 2, μ² = 0.536; |E₁(z) + γ + ln z| ≤ z gives ≤ 0.0051; the T0-tail residue ≤ c/8·(3/4) ≈ 0.0006). The single irreducible analytic input for BOTH lemmas: **explicit two-sided bounds on e^{−z}I₀(z) vs (2πz)^{−1/2}** — exactly the "self-contained Bessel-tail estimate" F028 predicted. Both lemmas now have clean, referee-ready statements; this is the recommended next analytic pass on the deterministic side (it would make the uniform floors N*_C ≥ 8 / N* ≥ 7 unconditional).

## 5. Status changes
(1) The M4 Tier-2 pin is now TWO named lemmas with measured margins (×2.83 on Lemma B's constant; ×2.45 finite / ×2.10 envelope overall) instead of one scalar inequality — strictly better-localized than after F028. (2) D∞ digits refined + integral representation deposited; the (A/2)ln6 normalization offset enters CONVENTIONS (quoting D-constants across α/β forms is a live unit trap of the factor-2 species). (3) The finite-D ridge (L = 47) is new structural data: certificates on real lattices live ≥ 2.4× below the threshold, uniformly. (4) Uniform floors remain CONDITIONAL (on Lemma A + Lemma B, or F028 form B) — Tier-1 finiteness stays the only unconditional ceiling statement. Grounds: §1 gate-backed; §2 derived-and-machine-verified (float-grade); §3 gate-backed + documented caveats; §4 derivation sketches, unverified; opinions labeled.
