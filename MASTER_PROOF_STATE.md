# WORKHOUSE — Master Proof State

**Rev. 4, 2026-08-22.** Final revision of the day. Both adversarial passes are complete
(24 + 14 agents). Rev. 1 (`OFF_AXIS_LEDGER.md`) is absorbed as Part II.

**Scope.** Exact lattice structure → the finite-order C2 dispute → the carrier-to-particle
bridge, after reviewing the dossier *"From the WORKHOUSE carrier to an actual particle"*.

**Labels.** **[V]** verified by me. **[R]** agent-reported *and* adversarially re-verified
against sources. **[R!]** reported, not re-verified. **[X]** retracted — kept with how it failed.

**Revision history, because it is evidence.** Rev. 2 claimed Schor had already done the
dossier's Phase A; Rev. 3 retracted that; Rev. 4 corrects Rev. 3 again. The oscillation is
itself the finding: **every first-pass literature reading in this session was wrong in one
direction or the other, and only the adversarial re-reads of the primary sources settled it.**

---

# Part 0 — The novelty question, finally settled

Three passes converged on this. The published record splits by **character type**, which is
the variable everyone kept dropping:

| | full-space exhaustiveness / upper gap | carrier-subspace masses |
|---|---|---|
| **real** χ, d=3 | **Schor**, NPB 231 + CMP 92 | — |
| **complex** χ (SU(3)), d=3 | **none** | O'Carroll–Braga–Schor 1985 |
| SU(3), d=4 | **none** | **none** |

Schor CMP 92 p.369 states exhaustiveness outright — the lowest excitation "**is the only
spectrum (besides the vacuum) up to the threshold −6(1−ε) log β**" — and CMP 92 then extends
the window to −7(1−ε)log β "**for arbitrary φ**", executed by decomposing the zero-momentum
space into four selection sectors and covering all of them (Thms 2.1–2.6). NPB 231's "new
lower bound for the upper mass gap" *is* a gap-above-the-shell theorem. **[R]**

But all of it assumes "the character χ is real irreducible and the space-time dimensionality
is three". SU(3)'s character is complex. And OBS85, which does handle SU(3), analyses the
spectrum "**restricted to the subspace generated by the plaquette function and its complex
conjugate**" — Theorem A lives inside that two-dimensional-generated subspace and **excludes
nothing else from the window**. **[R]**

**Conclusion.** The dossier's Theorem 1 is a *full-block* classification for SU(3). There is a
published antecedent **in kind** (Schor, and for the far harder interacting theory), and none
**in case**. The shell numbers themselves are folklore — Dahmen (hep-lat/9412080) gives
SU(2) 2+1D one-plaquette at 4·(3/4), six-link at 6·(3/4), gap two link quanta; substituting
SU(3)'s 2/3 gives 8/3, 4, 4/3 verbatim; Ciavarella–Klco–Savage (2101.10227 §II) give the SU(3)
one-plaquette energy and the color-parity split `|R±⟩=(|R⟩±|R̄⟩)/√2`. **[R]** What is new is
the proof, for SU(3), that nothing else lies below 4.

---

# Part I — The exact lattice core

```
S(k)+4I = B(k)B(k)^dag,  spec S = {-4, -4+q_a, -4+q_a},  psi = (d3bar,-d2bar,d1bar)
dim ker(partial_2) = L^3 + 2 on T_L^3 ;  H_eff,- = E_flat(u) I + t(u) B B^dag + O(u^4)
```

The flat band is **singular at Γ** — the three branches meet there. That is why Part III
cannot work with a rank-one object.

## I.1 The electric-shell theorem

**Arithmetic verified [V]:** Casimir numerators below 18 are `{0,4,9,10,16}`; smallest nonzero
2/3, next 3/2; `E_F = 8/3`; 1×2 rectangle `= 4`; gap `4/3`.

**Combinatorics verified by enumeration [V]** — every connected ≤5-edge min-degree-2 support
on T_L³, L = 3…7:

```
  L=3: contractible 4, winding 21, branched 0      L=6: contractible 4, winding 0, branched 0
  L=4: contractible 4, winding  1, branched 0      L=7: contractible 4, winding 0, branched 0
  L=5: contractible 4, winding  1, branched 0
```

**Zero branched supports at any L** — confirming the K₄∖e step empirically rather than by the
dossier's loose "theta graph needs ≥6 edges" argument.

### I.1a Strengthening [V]

A winding cycle needs ≥ L edges, so:

> **Theorem 1′.** For **L ≥ 6**, in the gauge-invariant C=− space with **no center-charge
> restriction**, `Spec(H₀) ∩ (−∞,4)` is exactly the 3L³ one-plaquette shell at 8/3, gap 4/3
> sharp. For L ∈ {3,4,5} the same holds after restricting to trivial Z₃ one-form charge.

The center-charge block is thus not doing work where the chain needs it — it buys uniformity
down to L=3, and the chain wants L → ∞.

### I.1b WORKHOUSE never asserted the claim §2 refutes **[R]**

Two independent exhaustive searches, including all 19 corpus transcripts (3.2 MB): the corpus
nowhere states `Q(H₀−8/3)Q ≥ (4/3)Q` or any equivalent. §2 most likely corrects an earlier
draft of the dossier itself.

`4sin²(π/L)` is unrelated: it is an **adjacency** level, `O(u²)` and `O(L⁻²)` in physical form
(`Δ⁽²⁾ = 4t_N u² sin²(π/L)`), vanishing at u=0 where the 4/3 lives. It is also **T3** — the
whole homology suite is two checks, both on `dim Z₂ = L³+2`. And §3.1 *declares* the
projection rather than deriving it: "The construction concerns the effective Hamiltonian
projected to the degenerate one-plaquette sector."

## I.2 The Z₃ one-form symmetry **[R]**

Exact, commuting with `H(u)` at **all** u (each `V_i` is a product of central single-link
multiplications). This is 't Hooft electric flux in Hamiltonian dress.

- **Caution the dossier omits:** this is the *spatial* one-form symmetry of the Hamiltonian,
  not the thermal Polyakov-loop center symmetry. Routinely conflated.
- **In its favour:** since `[V_i,H(u)]=0` exactly, the Riesz projector commutes with `V_i` and
  its **rank splits sector by sector** — so the center restriction is *not* an extra hazard for
  the Yarotsky step the way the gauge restriction is.

**[X] A torelon contaminant I reported in Rev. 3 does not exist.** A first-pass agent quoted
Meyer as putting the imaginary part of the N-torelon operator in `T₁^{+−}`. Meyer writes
`T₁^{−−}` — the parity superscript was flipped by a glyph-extraction error, confirmed against
both v1 and v2 of hep-lat/0412021. The physics confirms Meyer: parity reverses a *winding*
loop, so same-direction torelons give (−,−); parity maps a contractible planar plaquette to a
translate of the same orientation, which is why the carrier is P=+, C=−. **Different channel.**

**And Meyer measures the carrier's own channel and reports it clean [R]:** in `T₁^{+−}` the
masses "increase monotonically as the volume becomes smaller… at the 10% level at L ≃ 0.88 fm",
and the states "connect smoothly with their infinite-volume counterparts". That is a published
positive result for exactly the channel this program lives in.

---

# Part II — Finite order: the C2 dispute

## II.1 Standing

`A = 5/48` sealed, `B = D = 0`, `C` disputed: balanced `−0.023672320681`, historical
`−0.048086383181`, v10a.26 `−0.020213328886`. Both sides are **single computations sharing a
Stage-0 geometry compiler**; the extraction is **exact**; `−25/64` is a subtraction, never a
contraction. **[R]**

## II.2 Provenance, sharpened **[R]**

- The three kernel copies are **two executions of one program** (`VERSION 2026-06-13-stage3j-v1`),
  not independent computations. **[X — Rev. 1/2 cited the two Stage-3I hashes as independence.]**
  But the "alt" notebook is the executed copy, with a genuine cold Stage-0→3J run recorded.
- **`q_band⁽⁴⁾` does not come from that run.** Its numerator occurs zero times in the stage3j
  program, which evaluates seven parity points (Γ not among them). `q` comes from a separate
  "EXACT k=0 CERTIFICATE" and arrived **later, as a correction** to an extrapolation that
  "mistakenly used the rigid sub-component −2.5134".
- **`C_old` appears in no June artifact** — all 35 files carrying its numerator are August.
  **Nothing in `corpus-import/` computes it**; the only code deriving it from records is this
  repository's own `payloads.py`. It is an August re-coordinatization built for comparison
  against the v10a fit.
- The **nine-family allowlist has an executed empirical origin**: `NB_O4_hodge_v10a2…ipynb`,
  `PASSED 17/17 v10a.2 GATES`, patterns accumulated over 4,524 networks and 41,266 pair tests
  — genuinely empirical, not circular.
- `C_new` is a nine-family number, but **[X]** my reason ("33/33 shapes") was a non-sequitur —
  33/33 is the *cluster* path. The real proof is source ordering: `H4_ax_cols` built L6713 and
  extracted L6716, the eleven-family contractor not defined until L6861/L6918, `_qcache` bound
  once at L6145 to the nine-family routine.

## II.3 Retractions

**[X] The section-[17] causal claim.** The zero-sum C-row of `M⁻¹` is real — I verified
`[−1/2,(1+√5)/4,(1−√5)/4,0]` — but it describes a perturbation that never happened. Anchor
faces have displacement (0,0,0), carrying phase 1 at *every* k, so `d = vals − epsG` is
unchanged and **all four** coefficients are invariant. Certificate: A moved 6e-15, B 1.3e-15,
D 2.2e-14, C 4.6e-15 — all roundoff. Had `d` genuinely shifted, A would have moved **+8.085**,
B **−5.850**, D **+17.55**. **[R]**

**[X] "The whole balanced side is T3."** Three T1 checks exist (Q₃₂ Newton, P₄₀₂ Newton of
`B_N·D₄₀₉` with 424 exact coefficients, N=7…18 q samples). Only `β₃^bal`, `−25/64` and `Δq₃`
are T3 — all anchored at base N=7.

**[X] "No N=3 balanced contraction exists."** The balanced **local** layer *was* evaluated at
N=3 and matched the SU(3) `(p,q)` engine on all 140 balanced signatures, zero mismatches. Only
the **global** contraction to `β₃^bal` is missing. **[R]**

**Against my own caution:** a direct rank-4 balanced computation reproduces the N=4
continuation exactly, `q₆^bal` comes from a direct 35,130-path rerun, and the
`P₄₀₂/D₄₀₉ → P₁₇/R₂₀` reduction was checked exactly at 900 consecutive integers (both sides
degree 443). The continuation **is** tested against direct fixed-rank work. **[R]**

**Unchanged:** the balanced side's originating symbolic contraction generator is absent — the
corpus's own review calls it "Critical reproducibility gap 1".

## II.4 The dossier's §5A correction, accepted **[V]**

The MIXED block is `cos k_n(cos k_i + cos k_j)`, for which `C = −A/4` identically, so

```
rho := C + A/2 = A_MIXED/4 + C_INPLANE + C_ROTATION      (exact)
```

The mixed block enters at `A_MIXED/4 = −x`, **0.032% of the gap** (it needs 3119× its
historical value, with `A_NORMAL` flipping sign, to matter). But the deeper point stands
without mitigation: **I have record-level access to the historical kernel only.** The channel
localisation is a research-targeting result, not a proof premise.

## II.5 Scope and the decisive test

`δε₄ = 4ΔC_shp·e₂/q_a` vanishes at Γ and on every axial cut, so **C2 is independent of Part I
and of the Part III island**. Robustness **[V]**: `A + (4/3)C_shp` = 0.040051 / 0.072604 /
0.077216 — none destabilises the Γ minimum.

**The test:** contract the **42 exceptional signatures**. Census verified **[V]**: 182 = 140
balanced (`(1,1):30, (2,2):90, (3,3):20`, matching the stable-rank count) + 42 exceptional
(`(0,3):40, (0,6):2`). Prediction `Δw₍₁,₁,₀₎ = −25/256`, `Δw₍₂,₀,₀₎ = 0`; `25/256` occurs nowhere.

---

# Part III — The bridge

## III.1 Yarotsky — verdict: sound **[R]**

The second pass pulled the arXiv **LaTeX source** and overturned the first pass's "overstated".
Every transcription is verbatim. Theorem 1 permits "a Hilbert space H_x (possibly
infinite-dimensional)" and "h_x … non-negative self-adjoint, possibly unbounded … with a
spectral gap ≥ 1"; its conclusion is literally the dossier's patches. The first pass had
doubted whether the *published* companion covers this hypothesis class, reasoning from its
title ("quantum **spin** systems") — **a paper judged from its title, the exact error it was
hunting.** Crossref's abstract for that companion states inclusion (4) as its content, and
Henheik–Teufel–Wessel (*Lett. Math. Phys.* 112:1, 2022) independently set up "possibly infinite
dimensional" H_x and "possibly unbounded" h_x and write "**Yarotsky has proven in
[yarotsky2004perturbations]**…". The refereed literature reads it as covering exactly what the
dossier needs.

`θ_F = 1/33` re-derived independently twice (mine **[V]** and the verifier's), against `1/5`
from the block spectrum — and the dossier is right to use the **full** one-link semigroup,
since Yarotsky's hypotheses hold only on `⊗_e L²(SU(3))`.

**Two riders [R]:** the inclusion is for the **renormalized** `H̃_Λ = H_Λ − E_Λ`; and it is
**not scale-covariant** (LHS degree 1 in energy, RHS degree 2), so the contour half-width
`(1−33λ)/12` is in unrescaled units — `(1−33λ)/8` in Yarotsky units. A live arithmetic hazard.

**[X] My "rank 3L³ is not a Yarotsky import" charge was mis-aimed.** Theorem 1 indeed has no
projection and no dimension count — but the dossier never claims otherwise: it says Yarotsky
supplies the constants and the inclusion, then *itself* computes `θ_F`, sets the contour,
bounds the resolvent, and concludes the rank. That is Yarotsky's own §3 method. What is
genuinely missing is one paragraph — rank-constancy in u along the segment, plus
`mult_block(8/3) = 3L³` at u=0 — a completion, not an error.

### III.1a The domain, from both ends **[V]**

```
NECESSARY   (c_2-free):  E_flat = 8/3 + u + ... moves by ~u, patch half-width is lambda*(8/3),
                         so lambda >= 3u/8 ; with lambda < 1/33  =>  u < 8/99 = 0.0808
SUFFICIENT  (estimate):  ||chi+chibar|| = 6, 3 plaquettes/cell, x3/2 rescale => eps(u) <= 27u
                         lambda = c_2 eps(u) < 1/33  =>  u < 1/(891 c_2) ~ 1.1e-3  (g_H > 5.46)
```

The true domain lies between. Since `u = 1/g_H⁴` and the continuum is `g_H → 0`, i.e.
`u → ∞`: **the rigorous fixed-spacing domain and the continuum limit sit at opposite ends of
the coupling axis, with nothing connecting them.**

## III.2 The rank-3 cluster — right conclusion, and I had the wrong reason

The dossier is right that a rank-one contour is not justified: the three branches meet at Γ
and the minimum `q_a` is `4sin²(π/L)`.

**[X] My Rev. 3 "sting" was a category error.** I wrote that an external Riesz contour "can
never resolve inside the cluster". **A Riesz projection onto an isolated cluster is exact, not
resolution-limited** — cluster-isolate-then-diagonalize-inside is the standard use, and is
what the corpus itself does. The genuine obstruction is different: the isolation *inside* the
cluster is `t(u)·4sin²(π/L)`, and the corpus records that this "scales as L⁻² and collapses as
L → ∞". **That** defeats volume-uniform transport of the b₂-fold level. **[R]**

## III.3 Pole persistence (Theorem 2) — correct, and correctly audited

The `s=0` clause genuinely secures no-escape-of-mass (locally uniform convergence including 0
forces the limit transform continuous at 0, hence `‖ν‖ = L(0) = lim‖ν_ξ‖`; `δ_ξ` is exactly
the counterexample it excludes). Closed-set Portmanteau carries the atom bound, open-set kills
the annulus. **The theorem is true** — and it is Feller's extended continuity theorem plus
Portmanteau plus continuity from above, "with nothing gauge-theoretic in it". **[R]**

Its difficulty is entirely in its hypotheses, and one of them — `ν_ξ(I_ξ) ≥ z_* I₃` — **is
exactly what G18 exists to prove.** Theorem 2 assumes the bridge rather than building it.

## III.4 Spin — the argument fails at its premise **[V] + [R]**

Subduction, computed exactly **[V]** and independently re-derived by the verifier: T₁
multiplicity in `D^J↓O` is J=0:0, **1:1**, 2:0, **3:1**, **4:1**, **5:2**, 6:1, 7:2, 8:2, 9:3,
10:2, 11:3, 12:3.

> **T₁ is missed only by J = 0 and J = 2. It sits inside J = 1 and every J ≥ 3.**

**The residue clause is vacuous.** T₁ is absolutely irreducible with real character, so its
real commutant is ℝ and `ν(B) = c(B)·I₃` for **every** Borel set. `z_G I₃` is forced by cubic
symmetry alone, at every energy, for J = 1, 3, 4, 5 indifferently. Bookkeeping, not evidence.

**And the axial-vector premise is false for this source.** With `O_i = Σ_x Im Tr U_{jk}(x)`
and `Im Tr e^{iX} = −P₃/6 + …`, the leading continuum operator is `~ Tr F_{jk}³` — the
**diagonal `S_iii` of a symmetric rank-3 spatial tensor**, not an axial vector.
`Sym³(ℝ³) = 10 = J=3 (7) ⊕ J=1 (3)` contains **exactly two T₁ copies** **[V]**, and both reach
the diagonal triple, so **the carrier couples to `J = 3^{+−}` at leading order in a**. The
corpus says so in its own words: "the leading cubic C-odd tensor". **[R]**

So the J=3 contamination is not hypothetical — it is present at leading order, and the states
that would have to be excluded are precisely those with identically zero carrier overlap
(`⟨A₂|O_i|Ω⟩ = ⟨T₂|O_i|Ω⟩ = 0`, since `T₁⊗A₁` contains neither). **"1⁺⁻" has two proved
letters and one conjectural digit.**

## III.5 The gap ledger **[R]**

**Discharges nothing the ledger tracks.** G18's first half is untouched and *assumed*
(Theorem 2's `z_*`); its second half is attacked conditionally but defeated by the L⁻² collapse
of internal isolation. G17 unmentioned. G11 untouched. G19 untouched but **better posed** —
three named uniformities versus "nothing reaches the scaling window". **G23: a real structural
advantage** — the dossier escapes G23's diffusion-time-vs-Euclidean-time splice entirely,
because its Laplace transforms are OS-time from the start.

One **unregistered discharge**: UNIFIED §11.4's shell-four/shell-six shift `−(3/4)g² = −4/3`,
where `−3/4 = 1/(8/3 − 4)` is the reciprocal of the gap, is caveated as exact only "inside the
declared retained-space construction". Theorem 1 proves that truncation is complete below 4.

---

# Part IV — The blind spot, corrected **[V]**

```
literature/index.yaml (88 entries): Schor 0 | O'Carroll 0 | Osterwalder 0 | Seiler 0
                                    Yarotsky 0 | Dahmen 0 | Elitzur 0 | "one-form" 0
```

**[X] But "Yarotsky appears nowhere in WORKHOUSE" was wrong.**
`corpus-import/literature/LIT_SU3_compass_artifact_….md` cites **four** Yarotsky papers
including the exact `math-ph/0411042`, describes it as constructing "the quasi-particle /
dispersion picture from isolated eigenvalues of the decoupled model", and its Recommendation 2
already proposes the dossier's route. **[R]**

**And it contains the error that probably killed the route.** Its Item 1 header reads:
*"Yarotsky's theorems are the closest existing 'classical-perturbation' template, but are
stated for **finite-dimensional sites**."* Per III.1 that is false — the paper explicitly
permits infinite-dimensional `H_x`. **The corpus found this route, mis-assessed it as blocked,
and dropped it.** That is the most actionable single line in this document.

---

# Part V — Status

| Statement | Status |
|---|---|
| Incidence factorisation, Betti count, `t_N`, O(u³) | **T0/T1, solid** |
| Shell classification below 4, gap 4/3 | **sound**, enumeration-verified |
| Same, no center restriction, **L ≥ 6** | **sound, new here** |
| Exhaustiveness, real χ, d=3, interacting | **published 1983–84** (Schor) |
| Exhaustiveness, SU(3), any d | **no antecedent** — the dossier's contribution |
| Carrier-subspace masses, SU(3), d=3 | **published 1985** (OBS) |
| C-conjugation decomposition, N≠2, d=4 | **published 2025/26** (Faria da Veiga–O'Carroll) |
| `4sin²(π/L)` | **T3**, unrelated to 4/3 |
| `C_shp` (C2) | **open**; neither side promoted |
| −25/64 as a contraction | **never done**; local N=3 layer matched, global absent |
| Yarotsky import | **sound**; hypotheses genuinely covered |
| Domain | `u < 8/99` necessary; `u < 1/(891c₂)` sufficient (est.) |
| rank 3L³ | derived by the dossier; one rank-constancy paragraph missing |
| Transport of the b₂-fold level, volume-uniformly | **defeated** by L⁻² internal collapse |
| Pole-persistence lemma | **true**; assumes G18's first half |
| T₁ ⇒ J=1 | **false**; carrier couples to J=3 at leading order |
| Uniform continuum mass, gap, residue | **OPEN — the decisive step** |

---

# Part VI — Next, ranked

1. **Re-open the compass artifact and correct its Yarotsky assessment.** The corpus already
   found this route and rejected it on a false premise. Cheapest high-value move in the file.
2. **Index the missing literature** — Schor (NPB 222/231, CMP 92), OBS (CMP 97),
   Faria da Veiga–O'Carroll (2509.03513), Osterwalder–Seiler, Yarotsky, Dahmen,
   Ciavarella–Klco–Savage, Meyer (hep-lat/0412021). 2509.03513 corroborates the program's
   foundational C=− statement from outside; Meyer supplies measured `T₁^{+−}` volume behaviour.
3. **Register Theorem 1′ as a check.** Exact, enumerable, the repository's first T1 statement
   about the *electric* spectrum; it also retro-justifies UNIFIED §11.4's truncation.
4. **Contract the 42 exceptional signatures** (II.5).
5. **Face the J=3 problem.** The carrier is a cubic tensor source, not an axial vector. Either
   find a source with clean `J=1` projection, or accept that the continuum statement is about
   a `T₁^{+−}` *channel*, not a spin-1 particle.
6. **Ask what obstructs OBS85's carrier-subspace machinery in d=4 for the isolation half.**

---

## Provenance

**[V] mine:** Casimir ladder and shell energies; `θ_F = 1/33` and the contour distance; the
L=3…7 enumeration and the L ≥ 6 strengthening; the cubic subduction table; `Sym³` and the two
T₁ copies; the curvature triple; the exact `rho` identity and the 3119× figure; `ε(u) ≤ 27u`
and `u < 8/99`; the arXiv:2509.03513 abstract; the index.yaml counts and the corpus Yarotsky
citation.

**[R] re-verified:** the Schor/OBS character-type split; Yarotsky's hypotheses and the
Henheik–Teufel–Wessel corroboration; the C2 provenance chain; the section-[17] refutation; the
balanced-side T1/T3 split; the Meyer parity correction and the `T₁^{+−}` volume data; the
ledger map; the `Tr F³` source identification.

**[R!] not re-verified:** the Rothe textbook quotation (World Scientific returned 403).
